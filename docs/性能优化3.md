# 性能优化3：面向高分项目差距的大优化路线

本文档接替 `性能优化.md` 和 `性能优化2.md`，用于记录 perf3 阶段的调研、路线、实现顺序和测试数据。前两轮已经证明：只补零散 IR 小规则，分数很难从约 19 拉到五六十分；SCEV、unroll、GCM 这类 IR 强优化如果缺少后端承载能力，也会被栈访存和寄存器压力抵消。因此 perf3 先按高分项目的完整优化体系拆解缺口，再按依赖顺序推进大优化。

## 0. 当前基线

| 项目 | 内容 |
| --- | --- |
| 仓库 | `/Users/fiwoouo/Desktop/comp/compiler` |
| 分支 | `perf-fvv` |
| 起始 commit | `1f1d204` |
| 当前稳定 commit | `3b015cf` |
| 本地环境 | `docker exec -w /workspace compiler-comp bash -lc ...` |
| 禁止事项 | 不运行会改文件的 `rustfmt` |
| 平台提交页 | 2026-05-19 通过 Chrome 复查：提交页 body 为空；排行榜详情页仍显示“当前的提交结果暂未生成您的排行榜详情！”；暂时无法取得最新平台分和逐用例明细 |
| 平台排行榜参考 | 用户给定约 19 性能分；排行榜邻近可见 `18.8227`、`20.2298`，前排为 `68.8488`、`66.5930`、`62.2482`、`58.5305` |
| 功能基线 | `compiler2026-riscv-function`：140/140 |
| 性能基线命令 | `PERF_REPORT_FILE=/workspace/Testing/perf3-baseline-current.csv TEST_ROOT=/workspace/Testing/contesttestcases CASE_RUN_TIMEOUT=180s CASE_COMPILE_TIMEOUT=180s CASE_LINK_TIMEOUT=180s MINIC_EXTRA_ARGS="-O1" bash scripts/run_ci_tests.sh -perf 2026_performance --compiler ./build-docker/compiler --show-failures` |
| 性能基线结果 | 60/60，timer 总和 23931 ms，wall 总和 31314 ms |

注意：这次本地基线的 `01_mm1=157 ms` 明显高于前一轮稳定值约 `78-85 ms`，说明宿主或 qemu 负载偏高。perf3 后续每个优化点需要优先和相邻同环境基线比较；同时保留 `性能优化2.md` 中 E3 稳定逐用例中位总和 `12700 ms` 作为历史参考。

## 1. 高分项目来源与状态

| 项目 | 排名/性能分 | 本地路径 | 来源 | 可用性 | 重点参考 |
| --- | ---: | --- | --- | --- | --- |
| 规约珞樱 | 1 / 68.8488 | 暂无 | 平台用户 `T2026104862010151` 可见，但个人项目页未列出可 clone 仓库 | 只能作为分数上界 | 暂无源码证据 |
| 脑瓜嗡嗡 | 2 / 66.5930 | 暂无 | 平台用户 `T2026100552010281`，公开搜索未找到仓库 | 只能作为分数上界 | 暂无源码证据 |
| 啊对对队 / Compiler2026-X | 3 / 62.2482 | `Testing/ref-dui` | `https://gitlab.eduxiji.net/T2026104952010247/compiler2026-x.git` | 已 clone | AST/IR 简化、标量寄存器保留、全局数组/标量寄存器、汇编优化 |
| Sisyphus | 4 / 58.5305 | `Testing/ref-sisyphus` | `https://gitlab.eduxiji.net/T2026104862010407/compiler2026-sisyphus.git` | 已 clone | 完整 IR pipeline、结构化循环优化、后端 StrengthReduct/InstCombine/RvDCE、寄存器分配 |
| Hycompiler | 非当前榜单队伍，作为 hw/高分实现参考 | `Testing/Hycompiler` | `https://github.com/Johnny-son/Hycompiler.git` | 已 clone | 和 Sisyphus 同源风格的完整优化链 |
| Iroha | 10 / 43.3713 | `Testing/ref-iroha` | `https://gitlab.eduxiji.net/T2026181232010187/Compiler2026-Iroha.git` | 已 clone | SCCP/GVN/DCE/LoopSimplify/LCSSA/LoopRotate/LICM、后端 coalescing/regalloc |

测试平台的 `contest_rank_more.jsp` 当前返回“当前的提交结果暂未生成您的排行榜详情”，所以暂时无法从平台拿逐用例 best/self 明细。

## 2. 我们已经做了什么

| 层级 | 已有内容 | 现状判断 |
| --- | --- | --- |
| 基础 CFG/SSA | `SimplifyCFG`、`Mem2Reg`、`TrivialPhi` | 可支撑常规 SSA 优化，但没有 LCSSA/dedicated exits |
| 标量清理 | `ConstantFold`、`LocalCSE`、`DCE` | 有基本块/支配范围 CSE 的一部分收益，但缺 SCCP、完整 GVN、ADCE、InstCombine 体系 |
| 函数优化 | `InlineSimpleFunction` | 只覆盖小纯函数，缺常量实参特化、late inline、递归/调用图驱动策略 |
| 内存优化 | `MemoryAlias`、`DLE`、`DSE`、`LocalStoreForward`、`SingleStoreAlloca` | 有保守 base/offset 和局部 store-to-load，但缺 DAE、InlineStore、只读数组/全局常量合成、跨阶段多轮内存清理 |
| 循环基础 | `LoopAnalysis`、`LoopCanonicalizePass`、`LICMPass` | 已有 preheader，但缺 LCSSA、LoopRotate、dedicated exits、RemoveEmptyLoop |
| 后端 | 基础 lowering、图染色、简单 strength reduction、去掉 no-op copy | 最大短板：只分配 `t0-t6/ft0-ft11`，不用 callee-saved 寄存器；call-live 值被迫 spill；缺机器级 DCE/InstCombine/coalescing |

## 3. 高分项目对比出的缺口

| 大类 | 高分项目做法 | 我们缺失的内容 | 为什么影响大 |
| --- | --- | --- | --- |
| Pipeline 分层 | Hy/Sisyphus 将结构化 pre-opt、flatten 后 scalar、mem2reg 后内存、循环、late inline、后端分层，并多轮 cleanup | 目前是一条短 pipeline，loop 后 cleanup 少，late inline/后端清理少 | 大优化产生的临时 IR 无法被后续 pass 吸收 |
| 标量全局优化 | Sisyphus/Hy 多轮 `GVN -> DCE -> RegularFold -> GCM -> GVN -> AggressiveDCE`；Iroha 有 `SCCP -> GVN -> DCE` 多轮 | 缺 SCCP、完整表达式值编号、ADCE、跨阶段 InstCombine | 常量传播和不可达分支收敛不足，循环优化前 IR 偏胖 |
| 内存优化 | Hy/Sisyphus 多轮 `Alias -> DSE -> DLE`，还有 `DAE`、`InlineStore`、`SynthConstArray`、`DeadGlobalStore` | 只做局部/保守 DLE-DSE，没有数组/全局常量和跨函数参数内存信息 | 矩阵、卷积、huffman 等用例被重复 load/store 拖慢 |
| 循环结构 | Hy/Sisyphus 有 `CanonicalizeLoop(lcssa)`、`LoopRotate`、`RemoveEmptyLoop`；Iroha 有 `LoopSimplify`、`LCSSA`、`LoopRotate`、`LICM` | 缺 LCSSA、dedicated exits、LoopRotate | SCEV/unroll/GCM 很难稳定落地 |
| 循环强优化 | Sisyphus 在基础循环上叠加 `SCEV`、`ConstLoopUnroll`、`ScalarReplace`、`RepeatInvariantReduction`、若干矩阵/模乘结构优化 | 之前尝试 SCEV/unroll/GCM 但因后端压力回退 | 热点循环没有把地址递推、循环不变量、短 trip count 真正降下来 |
| 后端承载 | Hy/Sisyphus RISC-V 有 `StrengthReduct`、`InstCombine`、`RvDCE`、更完整 RegAlloc；Iroha 有 coalescing、callee-saved 保存、post-RA cleanup | 我们只用 7 个 caller-saved GPR，call-live 一律 spill，缺机器级 DCE/Combine/coalescing | 这是 perf2 后期 IR 优化“看起来合理但性能下降”的直接原因 |
| 标量寄存器化 | ref-dui 显式选择高频局部标量、全局数组基址、全局 int 标量放入 `s1-s11/fs*` | 我们所有 IR result 都先落栈，寄存器分配也不用 `s*` | 对 `matmul`、`many_mat_cal`、`knapsack`、`conv2d` 这类循环热点影响巨大 |

## 4. perf3 总路线

顺序原则：先补“承载大优化的共同前置”，再做会显著改变 IR 形态的强优化；每个阶段都必须有功能测试和性能数据。

```text
E. 后端寄存器与机器级承载
  -> B. 标量全局优化闭环
  -> A. 内存优化升级
  -> C. 循环基础设施补全
  -> D. 循环强优化
  -> F. Pipeline 调度和多轮收敛
```

为什么 perf3 不直接先做 SCEV/unroll/GCM：

- perf2 已经验证，SCEV/GEP split、ConstLoopUnroll、GCM 都能过功能测试，但 timer 明显回退。
- 回退主因不是优化方向错，而是当前后端寄存器少、call-live 值强制 spill、IR result 落栈多，导致新增临时值变成更多内存流量。
- 高分项目共同点是后端能承接中端优化：更多可分配寄存器、机器级 DCE/InstCombine、coalescing 或显式标量寄存器化。

## 5. 阶段计划

| 阶段 | 大点 | 子点 | 前置依赖 | 参考实现 | 实现思路 | 预计收益 | 风险 | 验收标准 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| E1 | 后端结构优化 | GPR callee-saved register allocation | 当前图染色 RA、FrameLayout | `Testing/ref-iroha/crates/iroha/src/backend/regalloc.rs`、`Testing/ref-dui/codegen_riscv.cpp`、Hy/Sisyphus `RegAlloc.cpp` | 增加 `s1-s11` 物理寄存器；call-live GPR 只能着色到 callee-saved；按实际使用保存/恢复 `s*` | 对 call-heavy、循环热点和长 live range 用例预期 3%-10%；同时为 SCEV/unroll/GCM 铺路 | ABI 保存/恢复错误会导致大面积功能错；frame size 改动需谨慎 | 构建通过，功能 140/140，性能相邻基线有明确改善或至少无退化且 spill 明显下降 |
| E2 | 后端结构优化 | MachineDCE + InstCombine | E1 | Hy/Sisyphus `RvDCE.cpp`、`InstCombine.cpp`，Iroha `BDCE`、`InstCombine` | 删除无副作用 dead virtual defs；合并 `li+add`、`copy` 链；保守处理 call/memory/branch | 2%-8%，主要减少 E1 后遗留 move/li 和无用计算 | 误删 memory/call/branch 会错 | 功能全过，性能正收益，汇编指令数下降 |
| B1 | 标量全局优化 | SCCP/条件常量传播 | CFG/SSA、E2 cleanup | Iroha `sccp.rs`，Hy `RegularFold.cpp` | lattice 标记 undef/const/overdef；替换常量；删除不可达边后跑 CFG/Phi 清理 | 3%-8%，对常量开关、递归前判断、数组维度常量有收益 | Phi/CFG 更新复杂 | 功能全过，性能正收益，不引入大面积 IR verify 错 |
| B2 | 标量全局优化 | 完整 GVN + ADCE | B1、支配树 | Hy/Sisyphus `GVN.cpp`、`AggressiveDCE.cpp`，Iroha `gvn.rs`/`dce.rs` | 扩展现有 LocalCSE 到函数级表达式值编号；ADCE 从 terminator/call/store/return 反向标记 | 5%-15%，用于吸收后续循环优化产生的表达式 | 内存表达式 alias 边界要保守 | 功能全过，性能正收益，优化后 IR 指令数下降 |
| A1 | 内存优化升级 | InlineStore/跨块 store-to-load forwarding | B2、MemoryAlias | Hy/Sisyphus `InlineStore.cpp`、`DLE.cpp`、`DSE.cpp` | 用支配树和 alias 追踪最近 store；仅 exact/base+offset 且无 clobber 时替换 load | 2%-8%，对数组局部临时和参数槽有效 | 误判 call/store clobber 会错 | 功能全过，性能正收益，退化用例可解释 |
| A2 | 内存优化升级 | 只读全局/常量数组合成 | A1 | Hy/Sisyphus `SynthConstArray.cpp`、`HoistConstArray.cpp`、`DeadGlobalStore.cpp` | 识别无 store 的 global const/scalar/小数组；load 变常量或局部只读缓存 | 1%-5%，对 `crc`、`huffman`、常量表有效 | 初始化解析和数组 GEP 要保守 | 功能全过，性能正收益 |
| C1 | 循环基础设施 | LCSSA + dedicated exits + LoopRotate | B2、A1 | Hy/Sisyphus `CanonicalizeLoop.cpp`、`LoopRotate.cpp`，Iroha `loop_simplify.rs`、`lcssa.rs`、`loop_rotate.rs` | 在已有 LoopCanonicalize 上补 exit phi、dedicated exit 和 do-while 形态旋转 | 直接收益小，但为 C2/D1 前置 | CFG/Phi 修补风险高 | 功能全过，性能不明显退化；后续 SCEV 命中率上升 |
| D1 | 循环强优化 | LICM 升级 + ScalarReplace | C1、A1 | Sisyphus `LICM.cpp`、`ScalarReplace.cpp`，Iroha `licm.rs` | 使用 alias 允许 invariant load 外提；对循环内不逃逸标量/固定地址做寄存器化 | 5%-15%，对矩阵/卷积/背包类热点 | 寄存器压力，需要 E1/E2 承接 | 功能全过，性能正收益 |
| D2 | 循环强优化 | SCEV + 小常数 unroll | C1、B2、E1/E2 | Hy/Sisyphus `SCEV.cpp`、`LoopUnroll.cpp` | 识别 `i = i + c`、trip count、GEP 递推；只展开低 trip count/低 body cost/低 phi 数循环 | 5%-20%，但必须按成本模型限制 | 展开过度会回退 | 功能全过，性能正收益；若回退则记录并撤回 |
| F1 | Pipeline 调度 | 多轮 cleanup 收敛 | B/A/C/D | Hy/Sisyphus main pipeline | 在 inline/memory/loop 后按 `Fold/GVN/DCE/SimplifyCFG` 小轮次收敛，避免一次性堆 pass | 2%-8%，让各阶段收益稳定 | 编译时间增长 | 功能全过，性能正收益或编译时间可接受 |

## 6. 测试命令

构建：

```bash
docker exec -w /workspace compiler-comp bash -lc 'cmake --build build-docker -j'
```

功能测试：

```bash
docker exec -w /workspace compiler-comp bash -lc 'TEST_ROOT=/workspace/Testing/contesttestcases bash scripts/run_ci_tests.sh -asm Testing/compiler2026-riscv-function --compiler ./build-docker/compiler --show-failures'
```

性能测试：

```bash
docker exec -w /workspace compiler-comp bash -lc 'PERF_REPORT_FILE=/workspace/Testing/<阶段名>.csv TEST_ROOT=/workspace/Testing/contesttestcases CASE_RUN_TIMEOUT=180s CASE_COMPILE_TIMEOUT=180s CASE_LINK_TIMEOUT=180s MINIC_EXTRA_ARGS="-O1" bash scripts/run_ci_tests.sh -perf 2026_performance --compiler ./build-docker/compiler --show-failures'
```

## 7. 阶段汇总表

| 阶段 | commit | 优化内容 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 相对上一稳定基线 | 结论 |
| --- | --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| perf3-baseline-current | `1f1d204` | perf3 当前环境基线 | 通过 | 140/140 | 60/60 | 23931 | 31314 | 当前环境第一列 | 有明显宿主负载噪声，仅作相邻比较起点 |
| E1 | `753f77a` | 后端 `s1-s11` callee-saved GPR 分配；call-live GPR 改用 callee-saved 承接；按实际使用保存/恢复 | 通过 | 140/140 | 60/60 | 23828 | 31272 | -103 ms timer（-0.43%），-42 ms wall（-0.13%） | 小幅正收益；更重要的是解除后续 SCEV/unroll/GCM 的后端寄存器前置瓶颈 |
| E3 | `e4b5149` | InstructionSelector 基本块内 GPR SSA 值缓存；同块局部临时值用 virtual reg copy 代替 stack load/store；含 call/float 函数保守退回旧路径 | 通过 | 140/140 | 60/60 | 17751 | 25172 | 相对 E1 -6077 ms timer（-25.50%），-6100 ms wall（-19.51%） | 稳定大幅收益；命中高分项目“不要让临时值全落栈”的核心差距 |
| E3 current-env compare | `3b015cf` | 当前代码在同一宿主负载下重跑，作为 E3 后续实验相邻基线 | 通过 | 沿用 E3 | 60/60 | 9741 / 9893 | 13860 / 14048 | 相邻环境参考 | E3 后宿主负载明显降低；后续实验必须和这组同环境数据比较 |
| E4 | 本提交 | FPR local value cache 成本模型：只在无 call 且 float local-only 值不少于 4 个的函数启用，只缓存 FPR 不缓存 GPR | 通过 | 140/140 | 60/60 | 9847 / 9800 | 13928 / 13921 | 相对同时间 E3 `9943`：-143 ms（-1.44%） | 小幅但有边界的浮点局部收益；主要改善 `h-10-*` |
| E5 | 本提交 | 参考 Sisyphus `FunctionEquivalence`，识别纯 i32 helper 等价于 `and/or/xor/not`，调用点直接 lowering 到机器 bitwise 指令 | 通过 | 140/140 | 60/60 | 9177 / 9049 | 13555 / 13169 | 相对同时间 E3 `9943`：复跑 -894 ms（-8.99%） | 大幅正收益；`crc*` 和 `huffman-*` 热点消失 |
| E7 | 本提交 | 扩展 helper equivalence：对单基本块小型纯 i32 helper 识别 `0`、`a+b`、`a-b`、`-(a+b)`，调用点直接 lowering | 通过 | 140/140 | 60/60 | 8782 | 12739 | 相对 E5 复跑 -267 ms timer（-2.95%），-430 ms wall（-3.27%） | 正收益；主要消除 `crypto-*` 中退化成恒零的 `_or` helper call |
| E8 | 本提交 | 扩展 helper equivalence：受限自递归采样识别 FFT `multiply(a,b)` 为 `a*b % 998244353`，调用点 lower 为 64-bit `mul/rem` | 通过 | 140/140 | 60/60 | 8362 | 12209 | 相对 E7 -420 ms timer（-4.78%），-530 ms wall（-4.16%） | 明确正收益；`fft0/1/2` 热点从递归模乘降为机器乘余 |
| E9 | 本提交 | 扩展 helper equivalence：识别 signed `max/min` 小函数，调用点 lower 为 `slt/subw/mulw/addw` branchless select | 通过 | 140/140 | 60/60 | 8188 | 12055 | 相对 E8 -174 ms timer（-2.08%），-154 ms wall（-1.26%） | 正收益；`h-4-*` 的 `max` 调用热点明显下降 |
| E10 | 本提交 | CFG 标量清理：常量 phi/icmp/branch 中转块前驱直连，消除短路布尔物化块 | 通过 | 140/140 | 60/60 | 8010 / 8063 | 11986 / 12004 | 复跑相对 E9 -125 ms timer（-1.53%），-51 ms wall（-0.42%） | 正收益；`knapsack_naive-*` 递归基线判断直接分支 |
| E12 | 本提交 | 后端 Phi register lowering：GPR/指针 Phi 用稳定机器虚拟寄存器和边拷贝承接，避免循环回边经栈槽 store/load | 通过 | 140/140 | 60/60 | 7287 / 7912 / 7732 | 11159 / 11881 / 12006 | 三次中位相对 E10 复跑 -331 ms timer（-4.10%），-123 ms wall（-1.02%） | 稳定正收益；矩阵、transpose、`sl*` 和卷积热点访存明显下降 |
| E13 | 本提交 | Machine COPY coalescing：干涉图构建时对同类 COPY 源/目标跳过自身干涉边，让 RA 可同色并由 lowering 删除同寄存器 COPY | 通过 | 140/140 | 60/60 | 7196 / 7544 | 11070 / 11701 | 两次均值相对 E12 中位 -362 ms timer（-4.68%），约 -496 ms wall（-4.17%） | 正收益；主要改善 E12 后增加的机器 `COPY/mv` 压力，`shuffle1`、`sl2`、`matmul2` 收益明显 |
| E14 | 本提交 | Copy affinity coloring：记录 COPY 源/目标亲和边，着色时优先复用已着色 partner 的物理寄存器，进一步提高 no-op COPY 删除率 | 通过 | 140/140 | 60/60 | 7293 / 6822 | 11393 / 10906 | 两次均值相对 E13 两次均值约 -313 ms timer（-4.24%），-236 ms wall（-2.07%） | 正收益；`shuffle1`、`many_mat_cal-*`、`sl1`、`transpose2`、`crypto-1` 继续下降 |
| E15 | 本提交 | Cross-block compare-branch fusion：仅被条件分支使用的 `icmp` 不再物化 bool，跨块分支直接重建比较分支，并标记跨块比较操作数必须可 reload | 通过 | 140/140 | 60/60 | 6887 / 6674 | 10763 / 10484 | 两次均值相对 E14 两次均值约 -277 ms timer（-3.93%），-526 ms wall（-4.72%） | 正收益；`knapsack_naive-*`、`h-4-03`、`transpose2`、`shuffle1` 继续下降 |
| E16 | 本提交 | Compact SP-relative epilogue：小/中栈帧返回点直接从 `sp` 取回 `ra/fp` 并 `addi sp, sp, frameSize`，大栈帧保留旧 `fp`-relative 模板 | 通过 | 140/140 | 60/60 | 6656 / 6434 | 10672 / 10148 | 两次均值相对 E15 两次均值约 -236 ms timer（-3.47%），-214 ms wall（-2.01%） | 正收益；2026 perf 汇编 `mv` 总数 `2595 -> 1923`，返回路径更接近高分后端的 `sp` 栈帧模型 |

## 8. 当前基线逐用例数据

| 用例 | 状态 | timer_ms | wall_ms | compile_ms | link_ms |
| --- | --- | ---: | ---: | ---: | ---: |
| `01_mm1` | ok | 157 | 188 | 32 | 63 |
| `01_mm2` | ok | 512 | 551 | 23 | 44 |
| `01_mm3` | ok | 341 | 377 | 22 | 47 |
| `03_sort1` | ok | 156 | 204 | 33 | 49 |
| `03_sort2` | ok | 153 | 196 | 34 | 66 |
| `03_sort3` | ok | 147 | 189 | 30 | 42 |
| `conv2d-1` | ok | 2695 | 2713 | 51 | 44 |
| `conv2d-2` | ok | 737 | 756 | 52 | 46 |
| `conv2d-3` | ok | 244 | 262 | 51 | 42 |
| `crc1` | ok | 197 | 217 | 18 | 42 |
| `crc2` | ok | 197 | 215 | 20 | 45 |
| `crc3` | ok | 193 | 211 | 19 | 37 |
| `crypto-1` | ok | 605 | 621 | 75 | 44 |
| `crypto-2` | ok | 423 | 439 | 75 | 43 |
| `crypto-3` | ok | 247 | 266 | 93 | 51 |
| `fft0` | ok | 26 | 48 | 26 | 41 |
| `fft1` | ok | 197 | 217 | 27 | 67 |
| `fft2` | ok | 93 | 124 | 24 | 40 |
| `h-1-01` | ok | 103 | 121 | 22 | 40 |
| `h-1-02` | ok | 9 | 26 | 16 | 38 |
| `h-1-03` | ok | 605 | 622 | 15 | 39 |
| `h-10-01` | ok | 39 | 71 | 22 | 38 |
| `h-10-02` | ok | 85 | 131 | 22 | 37 |
| `h-10-03` | ok | 171 | 238 | 21 | 37 |
| `h-4-01` | ok | 87 | 108 | 22 | 55 |
| `h-4-02` | ok | 1 | 20 | 23 | 51 |
| `h-4-03` | ok | 1092 | 1110 | 58 | 50 |
| `h-5-01` | ok | 1 | 746 | 22 | 39 |
| `h-5-02` | ok | 1 | 758 | 25 | 38 |
| `h-5-03` | ok | 1 | 752 | 20 | 39 |
| `h-8-01` | ok | 1 | 771 | 26 | 41 |
| `h-8-02` | ok | 1 | 758 | 27 | 45 |
| `h-8-03` | ok | 2 | 829 | 26 | 43 |
| `h-9-01` | ok | 7 | 23 | 21 | 44 |
| `h-9-02` | ok | 5 | 23 | 19 | 44 |
| `h-9-03` | ok | 6 | 23 | 20 | 45 |
| `huffman-01` | ok | 364 | 383 | 33 | 48 |
| `huffman-02` | ok | 384 | 403 | 34 | 48 |
| `huffman-03` | ok | 359 | 377 | 32 | 44 |
| `knapsack_naive-1` | ok | 1594 | 1612 | 16 | 42 |
| `knapsack_naive-2` | ok | 1608 | 1626 | 17 | 38 |
| `knapsack_naive-3` | ok | 1587 | 1604 | 16 | 40 |
| `many_mat_cal-1` | ok | 1170 | 1195 | 29 | 46 |
| `many_mat_cal-2` | ok | 1211 | 1235 | 29 | 38 |
| `many_mat_cal-3` | ok | 1230 | 1254 | 28 | 40 |
| `matmul1` | ok | 251 | 287 | 26 | 48 |
| `matmul2` | ok | 885 | 939 | 43 | 44 |
| `matmul3` | ok | 479 | 541 | 25 | 41 |
| `optimization_scheduling1` | ok | 1 | 19 | 16 | 39 |
| `optimization_scheduling2` | ok | 1 | 19 | 17 | 37 |
| `optimization_scheduling3` | ok | 1 | 19 | 18 | 43 |
| `shuffle0` | ok | 25 | 213 | 24 | 45 |
| `shuffle1` | ok | 491 | 1463 | 21 | 43 |
| `shuffle2` | ok | 108 | 433 | 21 | 39 |
| `sl1` | ok | 552 | 573 | 26 | 50 |
| `sl2` | ok | 1031 | 1053 | 21 | 45 |
| `sl3` | ok | 69 | 89 | 20 | 40 |
| `transpose0` | ok | 45 | 64 | 20 | 42 |
| `transpose1` | ok | 77 | 99 | 19 | 42 |
| `transpose2` | ok | 871 | 890 | 19 | 39 |

## 9. perf3 执行记录

### E1 GPR callee-saved register allocation

状态：已实现，待本提交入库。

实现要点：

1. 给后端增加 `s1-s11` 物理寄存器名称，不使用 `s0/fp`。
2. 图染色 GPR 可用集合扩展为 `t0-t6 + s1-s11`。
3. `CALL` 的 live-out GPR 不再一律 spill，而是限制着色到 callee-saved 集合；FPR 暂时保持保守 spill。
4. 根据最终着色实际使用到的 `s*`，在 prologue 保存，在每个 return epilogue 恢复。
5. 该阶段不做 peephole，不做语义激进变换，只补后端 ABI 和寄存器容量。

预期：这是 ref-dui/Iroha/Hy/Sisyphus 都具备而我们缺失的共同前置。若功能正确，预计能减少 call-live spill 和循环长 live range 溢出；如果当前高负载环境掩盖收益，需要至少观察相邻两次性能或统计 spill/save 数量。

实测结果：

| 项目 | 结果 |
| --- | --- |
| 构建 | `cmake --build build-docker -j` 通过 |
| 功能测试 | `compiler2026-riscv-function` ASM 140/140 |
| 性能测试 | `2026_performance` 60/60 |
| 当前相邻基线 | `perf3-baseline-current.csv`：timer 23931 ms，wall 31314 ms |
| E1 最终验证 | `perf3-e1-sreg-validated.csv`：timer 23828 ms，wall 31272 ms |
| 相对变化 | timer -103 ms（-0.43%），wall -42 ms（-0.13%） |

已验证但暂不采用的变体：

| 变体 | 结果 | 结论 |
| --- | --- | --- |
| 仅 call-live 使用 `s*`，普通 GPR 仍限制 `t0-t6` | `perf3-e1-calllive-sreg.csv`：timer 23959 ms，wall 31538 ms | 比相邻基线回退，说明仅处理跨调用 live range 不够，普通长 live range 也需要更多 GPR |
| 将 `a0-a7` 加入可分配集合 | 功能测试从最小用例开始大量 mismatch | 当前后端没有完整 precolored/物理寄存器干扰建模，参数/返回值寄存器不能直接纳入 RA |
| 给 `a0-a7` 增加简单 physical-def forbidden color | 功能测试仍大面积 mismatch | 只禁止 live-out 颜色不足以覆盖实参构造、返回值和隐式使用边界；本轮撤回 |
| leaf 函数省略保存/恢复 `ra` | 功能测试 140/140，但 `perf3-e1-sreg-leaf-final.csv` timer 23965 ms | 本地性能回退，暂不合入；后续若做完整 frame/prologue 优化再重测 |

逐用例对比：

| 用例 | 基线 timer | E1 timer | Δtimer | 基线 wall | E1 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 157 | 155 | -2 | 188 | 182 | -6 |
| `01_mm2` | 512 | 516 | +4 | 551 | 559 | +8 |
| `01_mm3` | 341 | 357 | +16 | 377 | 398 | +21 |
| `03_sort1` | 156 | 155 | -1 | 204 | 199 | -5 |
| `03_sort2` | 153 | 157 | +4 | 196 | 202 | +6 |
| `03_sort3` | 147 | 150 | +3 | 189 | 196 | +7 |
| `conv2d-1` | 2695 | 2715 | +20 | 2713 | 2753 | +40 |
| `conv2d-2` | 737 | 737 | +0 | 756 | 755 | -1 |
| `conv2d-3` | 244 | 245 | +1 | 262 | 264 | +2 |
| `crc1` | 197 | 199 | +2 | 217 | 218 | +1 |
| `crc2` | 197 | 197 | +0 | 215 | 215 | +0 |
| `crc3` | 193 | 194 | +1 | 211 | 212 | +1 |
| `crypto-1` | 605 | 596 | -9 | 621 | 614 | -7 |
| `crypto-2` | 423 | 416 | -7 | 439 | 434 | -5 |
| `crypto-3` | 247 | 239 | -8 | 266 | 257 | -9 |
| `fft0` | 26 | 22 | -4 | 48 | 41 | -7 |
| `fft1` | 197 | 197 | +0 | 217 | 216 | -1 |
| `fft2` | 93 | 90 | -3 | 124 | 110 | -14 |
| `h-1-01` | 103 | 106 | +3 | 121 | 125 | +4 |
| `h-1-02` | 9 | 12 | +3 | 26 | 33 | +7 |
| `h-1-03` | 605 | 609 | +4 | 622 | 627 | +5 |
| `h-10-01` | 39 | 42 | +3 | 71 | 75 | +4 |
| `h-10-02` | 85 | 87 | +2 | 131 | 135 | +4 |
| `h-10-03` | 171 | 167 | -4 | 238 | 231 | -7 |
| `h-4-01` | 87 | 75 | -12 | 108 | 90 | -18 |
| `h-4-02` | 1 | 2 | +1 | 20 | 18 | -2 |
| `h-4-03` | 1092 | 1027 | -65 | 1110 | 1045 | -65 |
| `h-5-01` | 1 | 1 | +0 | 746 | 885 | +139 |
| `h-5-02` | 1 | 1 | +0 | 758 | 755 | -3 |
| `h-5-03` | 1 | 1 | +0 | 752 | 791 | +39 |
| `h-8-01` | 1 | 1 | +0 | 771 | 760 | -11 |
| `h-8-02` | 1 | 1 | +0 | 758 | 762 | +4 |
| `h-8-03` | 2 | 1 | -1 | 829 | 762 | -67 |
| `h-9-01` | 7 | 7 | +0 | 23 | 24 | +1 |
| `h-9-02` | 5 | 5 | +0 | 23 | 21 | -2 |
| `h-9-03` | 6 | 6 | +0 | 23 | 24 | +1 |
| `huffman-01` | 364 | 367 | +3 | 383 | 386 | +3 |
| `huffman-02` | 384 | 360 | -24 | 403 | 378 | -25 |
| `huffman-03` | 359 | 360 | +1 | 377 | 377 | +0 |
| `knapsack_naive-1` | 1594 | 1587 | -7 | 1612 | 1604 | -8 |
| `knapsack_naive-2` | 1608 | 1575 | -33 | 1626 | 1591 | -35 |
| `knapsack_naive-3` | 1587 | 1581 | -6 | 1604 | 1599 | -5 |
| `many_mat_cal-1` | 1170 | 1335 | +165 | 1195 | 1358 | +163 |
| `many_mat_cal-2` | 1211 | 1244 | +33 | 1235 | 1269 | +34 |
| `many_mat_cal-3` | 1230 | 1213 | -17 | 1254 | 1235 | -19 |
| `matmul1` | 251 | 245 | -6 | 287 | 278 | -9 |
| `matmul2` | 885 | 809 | -76 | 939 | 863 | -76 |
| `matmul3` | 479 | 469 | -10 | 541 | 516 | -25 |
| `optimization_scheduling1` | 1 | 1 | +0 | 19 | 19 | +0 |
| `optimization_scheduling2` | 1 | 1 | +0 | 19 | 18 | -1 |
| `optimization_scheduling3` | 1 | 1 | +0 | 19 | 33 | +14 |
| `shuffle0` | 25 | 26 | +1 | 213 | 216 | +3 |
| `shuffle1` | 491 | 425 | -66 | 1463 | 1382 | -81 |
| `shuffle2` | 108 | 110 | +2 | 433 | 418 | -15 |
| `sl1` | 552 | 535 | -17 | 573 | 555 | -18 |
| `sl2` | 1031 | 1029 | -2 | 1053 | 1050 | -3 |
| `sl3` | 69 | 74 | +5 | 89 | 94 | +5 |
| `transpose0` | 45 | 44 | -1 | 64 | 61 | -3 |
| `transpose1` | 77 | 77 | +0 | 99 | 95 | -4 |
| `transpose2` | 871 | 872 | +1 | 890 | 889 | -1 |

阶段判断：

- E1 的直接收益只有 -0.43%，低于最初对“单独阶段 3%-10%”的理想预期；原因是当前大部分热点仍由内存访存和中端未消除的重复计算主导。
- 这个阶段仍然值得合入，因为高分项目都具备 callee-saved/更大寄存器集合，而 perf2 已经看到 SCEV/unroll/GCM 在寄存器压力下回退。E1 是后续循环强优化的共同前置。
- 下一步优先做 E2 MachineDCE + MachineInstCombine：先消化 E1 带来的更多寄存器移动、冗余 `li/add/copy` 和死定义，再回到 SCCP/GVN/内存/循环。

### E2 MachineDCE + MachineInstCombine 第一轮

状态：已验证但暂不采用。

参考实现：

- Hy/Sisyphus：`Testing/Hycompiler/src/rv/InstCombine.cpp`、`Testing/Hycompiler/src/rv/RvDCE.cpp`。
- Iroha：`Testing/ref-iroha/crates/iroha/src/backend/inst_comibine.rs`、`Testing/ref-iroha/crates/iroha/src/backend/dce.rs`。

试验思路：

1. 在 selector 之后、图染色寄存器分配之前增加机器级清理。
2. DCE 删除无物理寄存器写、无 call/store/branch/ret 副作用且 virtual def 不再 live-out 的机器指令。
3. InstCombine 尝试 `li 0 -> zero`、`add/addi 0`、`copy` 前递、`li+add/sub -> addi/addiw` 等规则。
4. 普通 `a0-a7` 物理寄存器仍不进入 RA，避免破坏 ABI。

结果：

| 变体 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 相对 E1 `23828` | 结论 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| 完整第一版：DCE + 恒等式 + 普通 `copy` 前递 + `li+add/sub` | 140/140 | 60/60 | 24234 | 31739 | +406 ms | 回退明显；普通 copy 前递会拉长 live range，加重寄存器压力 |
| 去掉普通 `copy` 前递，保留恒等式和 `li+add/sub` | 140/140 | 60/60 | 23825 | 31281 | -3 ms | 首跑微弱正收益，但幅度小于噪声 |
| 仅恒等式 + DCE | 140/140 | 60/60 | 24339 | 32211 | +511 ms | 回退明显；当前图染色对机器级删改较敏感 |
| 仅 `li+add/sub -> addi/addiw` + DCE | 140/140 | 60/60 | 23956 | 31315 | +128 ms | 回退，不采用 |
| 去掉普通 `copy` 前递候选复跑 | 140/140 | 60/60 | 23847 | 31327 | +19 ms | 不能稳定优于 E1，不满足提交标准 |

判断：

- E2 方向仍然是高分项目必备能力，但当前机器 IR 不是 SSA，且有大量二地址式“同 vreg def/use”的地址更新。简单 replace-all-uses 或 DCE 会改变 live range 形态，容易影响图染色结果。
- 本轮不提交 E2 代码；保留结论：后续若再做机器级优化，应先补更精确的 def-use/use-count 和 coalescing/拷贝消除成本模型，而不是全局前递。
- 下一步切换到 B1 SCCP。理由是 SCCP 在 IR 层能先收缩控制流和常量分支，减少送入后端的机器指令规模，比当前 pre-RA 机器清理更可能稳定拉分。

### B1 SCCP 第一轮

状态：已验证但暂不采用。

实现范围：

1. 参考 Iroha `sccp.rs` 的方向，实现保守 sparse conditional constant propagation。
2. 入口块开始标记可达边；Phi 只合并 executable incoming edge。
3. 支持整数/浮点二元、`icmp/fcmp`、`zext`、`sitofp/fptosi/bitcast` 的 lattice 传播。
4. 在 `ConstantFold` 后、`LocalCSE` 前运行，后接 `SimplifyCFG` 处理常量分支。

结果：

| 变体 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 相对 E1 `23828` | 结论 |
| --- | --- | --- | ---: | ---: | ---: | --- |
| SCCP + SimplifyCFG 第一版 | 140/140 | 60/60 | 23868 | 31306 | +40 ms | 功能正确但性能未提升；不提交代码 |

判断：

- 当前 `ConstantFold + LocalCSE + SimplifyCFG` 已经吃掉了大部分简单常量；这个 SCCP 切片能证明的新增常量不足以抵消 CFG/IR 形态扰动。
- 真正的结构性短板仍是后端 selector 把大部分 IR 临时值写入栈槽，再从栈槽读回。高分项目要么有更强的后端寄存器化/合并，要么在 IR 后期显著减少临时值。
- 下一步切换到 E3：基本块内 SSA 值寄存器缓存/局部标量化。目标是在不改变跨块语义的前提下，先减少同一基本块内的临时值 store/load 对。

### E3 InstructionSelector 基本块内 GPR SSA 值缓存

状态：已实现，待本提交入库。

为什么做它：

- 高分项目的共同点不是只多几个 IR 小规则，而是后端不会把每个 SSA 临时值都强制落栈。
- 我们当前 lowering 对大多数 IR result 都 `storeValue(result, inst.result())`，后续使用再 `loadValue()`，即使同一基本块内马上使用也会形成冗余栈访存。
- 这个问题会放大循环优化和 GVN 的寄存器压力，解释了前面 SCEV/unroll/GCM 容易回退的现象。

实现边界：

1. 预扫描函数，记录每个 IR 指令所在基本块，以及所有 use 都在同一基本块内的 local-only SSA 值。
2. 在每个 IR 基本块开始时清空本地缓存。
3. `storeValue` 遇到“当前块定义的 GPR 指令结果”时，把源 virtual reg 记入缓存。
4. 如果该值 local-only，且当前块不含 call，则跳过栈槽 store；后续 `loadValue` 命中缓存时发一条新 vreg `COPY`，用 copy 代替 stack load。
5. 含 call 的基本块不跳过 store，call 后清空缓存；含浮点 IR 的函数完全关闭该缓存，避免当前 FPR/call ABI 边界不稳。
6. 不缓存全局变量、函数参数、跨边 Phi 结果；跨块使用仍走原栈槽路径。

已验证但暂不采用的变体：

| 变体 | 结果 | 结论 |
| --- | --- | --- |
| 直接返回缓存 vreg，不额外 copy | 功能 140/140，但 perf 集 `fft*`、`shuffle*`、`sl*` 错误；51/60，timer 15196 ms 不可比较 | 破坏二地址式 lowering：`zext`、GEP 递推会改写原 SSA 缓存值 |
| 命中缓存时 copy 到新 vreg，但所有函数都启用 | 功能 `39_fp_params` segfault，139/140 | 当前 FPR/call ABI 边界不够稳，浮点函数暂不启用 |
| GPR-only，含浮点 IR 函数禁用；含 call 块不跳过 store | 功能 140/140；perf 60/60；两次 timer 17818/17751 ms | 采用 |
| E3.1 在只读使用点直接使用缓存 operand，减少 copy | 功能 140/140；perf 60/60；timer 17933 ms，wall 25521 ms | 相对 E3 复跑回退 182 ms；减少 copy 的同时拉长 live range，暂不采用 |

实测结果：

| 项目 | 结果 |
| --- | --- |
| 构建 | `cmake --build build-docker -j` 通过 |
| 功能测试 | `compiler2026-riscv-function` ASM 140/140 |
| 性能测试首跑 | `perf3-e3-local-value-cache-gpr.csv`：timer 17818 ms，wall 25223 ms |
| 性能测试复跑 | `perf3-e3-local-value-cache-gpr-rerun.csv`：timer 17751 ms，wall 25172 ms |
| 相对 E1 | 复跑 timer -6077 ms（-25.50%），wall -6100 ms（-19.51%） |

逐用例对比（E1 vs E3 复跑）：

| 用例 | E1 timer | E3 timer | Δtimer | E1 wall | E3 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 155 | 105 | -50 | 182 | 137 | -45 |
| `01_mm2` | 516 | 353 | -163 | 559 | 396 | -163 |
| `01_mm3` | 357 | 230 | -127 | 398 | 266 | -132 |
| `03_sort1` | 155 | 114 | -41 | 199 | 159 | -40 |
| `03_sort2` | 157 | 113 | -44 | 202 | 156 | -46 |
| `03_sort3` | 150 | 112 | -38 | 196 | 154 | -42 |
| `conv2d-1` | 2715 | 1538 | -1177 | 2753 | 1556 | -1197 |
| `conv2d-2` | 737 | 438 | -299 | 755 | 455 | -300 |
| `conv2d-3` | 245 | 142 | -103 | 264 | 161 | -103 |
| `crc1` | 199 | 185 | -14 | 218 | 215 | -3 |
| `crc2` | 197 | 179 | -18 | 215 | 199 | -16 |
| `crc3` | 194 | 178 | -16 | 212 | 198 | -14 |
| `crypto-1` | 596 | 366 | -230 | 614 | 383 | -231 |
| `crypto-2` | 416 | 254 | -162 | 434 | 271 | -163 |
| `crypto-3` | 239 | 148 | -91 | 257 | 166 | -91 |
| `fft0` | 22 | 19 | -3 | 41 | 37 | -4 |
| `fft1` | 197 | 160 | -37 | 216 | 179 | -37 |
| `fft2` | 90 | 74 | -16 | 110 | 94 | -16 |
| `h-1-01` | 106 | 93 | -13 | 125 | 111 | -14 |
| `h-1-02` | 12 | 8 | -4 | 33 | 25 | -8 |
| `h-1-03` | 609 | 556 | -53 | 627 | 574 | -53 |
| `h-10-01` | 42 | 41 | -1 | 75 | 75 | +0 |
| `h-10-02` | 87 | 89 | +2 | 135 | 138 | +3 |
| `h-10-03` | 167 | 170 | +3 | 231 | 238 | +7 |
| `h-4-01` | 75 | 61 | -14 | 90 | 79 | -11 |
| `h-4-02` | 2 | 2 | +0 | 18 | 21 | +3 |
| `h-4-03` | 1027 | 810 | -217 | 1045 | 827 | -218 |
| `h-5-01` | 1 | 1 | +0 | 885 | 770 | -115 |
| `h-5-02` | 1 | 1 | +0 | 755 | 751 | -4 |
| `h-5-03` | 1 | 1 | +0 | 791 | 777 | -14 |
| `h-8-01` | 1 | 1 | +0 | 760 | 802 | +42 |
| `h-8-02` | 1 | 1 | +0 | 762 | 792 | +30 |
| `h-8-03` | 1 | 1 | +0 | 762 | 755 | -7 |
| `h-9-01` | 7 | 8 | +1 | 24 | 26 | +2 |
| `h-9-02` | 5 | 4 | -1 | 21 | 22 | +1 |
| `h-9-03` | 6 | 5 | -1 | 24 | 23 | -1 |
| `huffman-01` | 367 | 332 | -35 | 386 | 352 | -34 |
| `huffman-02` | 360 | 329 | -31 | 378 | 350 | -28 |
| `huffman-03` | 360 | 336 | -24 | 377 | 354 | -23 |
| `knapsack_naive-1` | 1587 | 1450 | -137 | 1604 | 1470 | -134 |
| `knapsack_naive-2` | 1575 | 1416 | -159 | 1591 | 1433 | -158 |
| `knapsack_naive-3` | 1581 | 1447 | -134 | 1599 | 1466 | -133 |
| `many_mat_cal-1` | 1335 | 825 | -510 | 1358 | 849 | -509 |
| `many_mat_cal-2` | 1244 | 799 | -445 | 1269 | 823 | -446 |
| `many_mat_cal-3` | 1213 | 796 | -417 | 1235 | 820 | -415 |
| `matmul1` | 245 | 159 | -86 | 278 | 209 | -69 |
| `matmul2` | 809 | 542 | -267 | 863 | 600 | -263 |
| `matmul3` | 469 | 304 | -165 | 516 | 351 | -165 |
| `optimization_scheduling1` | 1 | 1 | +0 | 19 | 20 | +1 |
| `optimization_scheduling2` | 1 | 1 | +0 | 18 | 20 | +2 |
| `optimization_scheduling3` | 1 | 1 | +0 | 33 | 19 | -14 |
| `shuffle0` | 26 | 17 | -9 | 216 | 207 | -9 |
| `shuffle1` | 425 | 474 | +49 | 1382 | 1442 | +60 |
| `shuffle2` | 110 | 89 | -21 | 418 | 404 | -14 |
| `sl1` | 535 | 377 | -158 | 555 | 396 | -159 |
| `sl2` | 1029 | 722 | -307 | 1050 | 743 | -307 |
| `sl3` | 74 | 48 | -26 | 94 | 68 | -26 |
| `transpose0` | 44 | 34 | -10 | 61 | 56 | -5 |
| `transpose1` | 77 | 59 | -18 | 95 | 82 | -13 |
| `transpose2` | 872 | 632 | -240 | 889 | 650 | -239 |

后续计划：

- E3 之后再回头做机器级 DCE/InstCombine，会更有意义：当前栈访存已经少很多，copy/冗余指令的收益会更容易稳定显现。
- 可以继续扩展为跨基本块 value cache 或简单 block-local coalescing，但必须先配套更精确的 live range 成本模型。
- 浮点函数暂不开启，后续应先补 FPR callee-saved/跨 call 策略，再试 FPR value cache。

### E3 后续扩展实验批次

状态：已验证但暂不采用；本批次均已回退代码，只保留测试数据和设计结论。

为什么要记录这些失败实验：

1. E3 之后总时间已经从 1.7-2.3 万毫秒降到当前同环境约 1 万毫秒，单条小规则很容易被宿主/qemu 噪声盖住。
2. 多个看似合理的 selector/inline 小优化在首跑微弱改善、复跑回退，说明下一步不能继续靠“想一条规则试一条规则”，必须回到高分项目的结构性缺口。
3. 这些实验给后续大优化提供边界：哪些 live range 会拉长、哪些 call/FPR/inline 方向需要成本模型。

同环境 E3 参考：

| 文件 | 状态 | timer 总和 | wall 总和 | 用途 |
| --- | --- | ---: | ---: | --- |
| `perf3-e3-currentenv-compare.csv` | 60/60 | 9741 | 13860 | E3 当前环境首个参考 |
| `perf3-e3-currentenv-compare-rerun.csv` | 60/60 | 9893 | 14048 | E3 当前环境复跑参考 |

已验证但不采用：

| 方向 | 变体 | 功能测试 | 性能数据 | 结论 |
| --- | --- | --- | --- | --- |
| 函数形参寄存器缓存 | 所有 GPR 形参跨函数缓存 | `32_many_params3` 编译超时，功能 139/140 | 不进入性能比较 | 栈传形参太多时会制造超长 live range，和当前后端成本模型冲突 |
| 函数形参寄存器缓存 | 仅缓存前 8 个寄存器传参 | 140/140 | `9747/13756`，复跑 `10223/14433` | 首跑接近 E3，复跑明显回退；不稳定 |
| GPR cache 放开浮点函数 | 仅无 call 的浮点函数启用 GPR local cache | 140/140 | `10127/14326` | 比 E3 同环境更慢；浮点函数里即使只缓存 GPR，也会改变寄存器压力和调度形态 |
| Inline 阈值 | `InlineSimpleFunctionPass` 阈值 32 | 140/140 | `9850/13953`，复跑 `9887/14022` | 没有稳定优于 E3；更多 inline 不等于更快 |
| Inline 阈值 | 阈值 16 | 140/140 | `9950/14012` | 回退，不采用 |
| Selector 常乘 strength reduction | `2^k±1` 和两个 bit 的乘法改为 shift/add/sub | 140/140 | `9738/13836`，复跑 `10022/14046` | 首跑只快 3 ms，复跑回退；属于噪声级小优化 |
| Late small inline pipeline | cleanup 后二次小函数 inline，再跑 fold/local-cse/dce | 140/140 | `9862/13975` | 能把 crypto `_or` 调用从 6 个降到 0 个，但总性能不升；缺调用图/热度/成本模型 |
| signed div/rem by power-of-two | 正 2 的幂除法/取模改为 bias + shift | 140/140 | `9894/14005` | 当前性能集命中少或增加指令；不采用 |
| FPR callee-saved | 增加 `fs0-fs11`，call-live FPR 使用 callee-saved | 140/140 | `9848/14180` | 单独阶段不优于 E3；保存恢复和 FPR 压力收益不抵成本 |
| FPR local cache | FPR callee-saved + FPR local value cache | 140/140 | `9861/14133`，复跑 `9609/13939`，三跑 `9943/14277` | 对 `h-10-*` 稳定局部有效，但总分噪声和回退风险较大；需要按函数成本模型启用 |

FPR local cache 的局部信号：

| 用例组 | E3 复跑 timer | FPR cache 复跑 timer | FPR cache 三跑 timer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `h-10-01` | 22 | 15 | 15 | 稳定改善 |
| `h-10-02` | 45 | 31 | 31 | 稳定改善 |
| `h-10-03` | 88 | 58 | 59 | 稳定改善 |

阶段判断：

- 形参缓存、直接缓存 operand、普通 copy 前递的共同问题是拉长 live range；它们不是方向错，而是缺少成本模型和 coalescing。
- FPR cache 已经证明“浮点局部值不落栈”有局部价值，但不能全局打开；后续若继续 E 类后端优化，应做按函数筛选：无 call、浮点 local-only 值多、FPR 压力可控、保存恢复成本低时才启用。
- Late inline 和 inline 阈值实验说明当前 `InlineSimpleFunction` 不足以吃到高分项目的主要收益。下一次若做 B/F 类函数优化，应该转向“常量实参特化 + 调用图驱动 late inline”，而不是单纯调阈值。
- 乘法/div-rem strength reduction 已经接近 peephole 范围，收益不稳定；perf3 不再把这类规则作为核心路线。

下一步路线调整：

1. 优先做一个结构性更强、能解释性能集热点的优化，而不是继续堆 selector 小规则。
2. 候选一：只读全局/常量数组分析。目标是 `crc`、`huffman`、常量表类用例，属于 A2，并且是高分项目 Hy/Sisyphus 的 `SynthConstArray/HoistConstArray/DeadGlobalStore` 方向。
3. 候选二：按成本模型启用 FPR local cache。目标是 `h-10-*` 这类浮点局部计算，用前述局部信号作为准入依据，避免全局回退。
4. 候选三：常量实参特化。目标是 crypto/crc 中的小 helper 和配置参数常量，替代简单提高 inline 阈值。
5. 选择顺序：先分析只读全局/常量数组是否在本项目 IR 中容易安全落地；如果命中不足，再做 FPR local cache 成本模型；最后再考虑常量实参特化。

### E4 FPR local cache 成本模型

状态：已实现，待本提交入库。

为什么做它：

- E3 后的失败实验显示，FPR local cache 对 `h-10-*` 稳定有效，但全局开启会受寄存器压力和保存恢复成本影响。
- 本轮不再增加 `fs0-fs11`，避免 ABI 保存恢复成本；只在函数无 call、浮点 local-only SSA 值数量不少于 4 时启用。
- 在启用函数里只缓存 FPR 值，不缓存 GPR 值。原因是此前“浮点函数里只放开 GPR cache”已验证回退，GPR live range 不能再被无条件拉长。

实现要点：

1. `InstructionSelector::analyzeLocalValues()` 统计函数是否含浮点值、是否含 call、local-only 浮点值数量。
2. 非浮点函数保持 E3 原有 GPR local cache。
3. 浮点函数默认仍禁用 local cache；只有 `!hasCall && localOnlyFloatValues >= 4` 时启用 FPR-only cache。
4. `storeValue()` 在 FPR-only 模式下只缓存 FPR SSA result，GPR result 仍走栈槽路径。

实测结果：

| 文件 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 结论 |
| --- | --- | --- | ---: | ---: | --- |
| `perf3-e4-fpr-local-cache-cost.csv` | 140/140 | 60/60 | 9847 | 13928 | 首跑通过，`h-10-*` 明显改善 |
| `perf3-e4-fpr-local-cache-cost-rerun.csv` | 140/140 | 60/60 | 9800 | 13921 | 复跑稳定在 E3 同环境区间内偏优 |
| `perf3-e3-currentenv-after-fprcost.csv` | 沿用 E3 | 60/60 | 9943 | 14017 | 同时间 E3 对照 |

关键用例：

| 用例 | E3 同时间 timer | E4 复跑 timer | Δtimer |
| --- | ---: | ---: | ---: |
| `h-10-01` | 23 | 16 | -7 |
| `h-10-02` | 47 | 34 | -13 |
| `h-10-03` | 91 | 62 | -29 |

结论：

- E4 是小幅但边界清楚的 E 类补丁，适合作为后续浮点后端优化前置。
- 它不是拉开 19 到五六十分差距的主力；真正的大收益还要来自 helper 等价识别、内存/循环强优化和 pipeline。

### E5 pure helper equivalence lowering

状态：已实现，待本提交入库。

参考实现：

- Sisyphus：`Testing/ref-sisyphus/src/opt/FunctionEquivalence.cpp`。
- Hy/Sisyphus 后端都有 `And/Or/Xor` 机器指令与 combine 支撑；我们的后端此前只有 `xor/xori/andi`，没有通用 `and/or` opcode，也不会识别手写 bitwise helper。

为什么它是大优化：

- `crc*` 中 `_and/_xor` 用 32 次循环模拟位运算；不识别时每个字节处理会反复调用 helper，成本非常高。
- `huffman-*` 也有可采样证明的纯 i32 helper，替换后热点从函数调用和循环模拟降成单条机器指令。
- 这类优化属于高分项目的“函数等价/语义识别”体系，不是单条后端 peephole。

实现要点：

1. 后端新增 `MachineOpcode::AND` 和 `MachineOpcode::OR`，汇编 lower 到 RISC-V `and/or`。
2. 在 `InstructionSelector` 中为 callee 做小型纯 i32 解释器，支持 `phi`、`br`、`ret`、整数算术、`icmp`、`zext`、`bitcast`。
3. 对 1/2 参数、返回 i32、指令数不超过 180 的 helper 进行采样分类。
4. 二元 helper 在正整数样本集上分别比对 `&`、`|`、`^`；一元 helper 比对 `~x`，包含负数样本。
5. 调用点命中后直接生成 `and/or/xor/xori/andi`，不改 IR，不删除 helper 本体；不能证明则保留原 call。
6. 对 `and/xor` 的 12-bit 常量右操作数走 `andi/xori`，例如 `crc & 255` 直接变 `andi`。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e5-helper-equivalence.csv` | 通过 | 140/140 | 60/60 | 9177 | 13555 |
| `perf3-e5-helper-equivalence-rerun.csv` | 通过 | 140/140 | 60/60 | 9049 | 13169 |

相邻对比：

| 对照 | timer | wall | E5 复跑 timer 提升 | E5 复跑 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E3 同时间对照 `perf3-e3-currentenv-after-fprcost.csv` | 9943 | 14017 | -894 ms（-8.99%） | -848 ms（-6.05%） |
| E4 复跑 `perf3-e4-fpr-local-cache-cost-rerun.csv` | 9800 | 13921 | -751 ms（-7.66%） | -752 ms（-5.40%） |

关键用例收益（E3 同时间对照 -> E5 复跑）：

| 用例 | E3 timer | E5 timer | Δtimer | 原因 |
| --- | ---: | ---: | ---: | --- |
| `crc1` | 93 | 1 | -92 | `_and(crc, 255)` 变 `andi`，`_xor` 变 `xor` |
| `crc2` | 91 | 2 | -89 | 同上 |
| `crc3` | 93 | 1 | -92 | 同上 |
| `huffman-01` | 173 | 22 | -151 | helper call/loop 被 bitwise 指令替代 |
| `huffman-02` | 176 | 22 | -154 | 同上 |
| `huffman-03` | 174 | 22 | -152 | 同上 |
| `h-10-01` | 23 | 16 | -7 | 继承 E4 FPR-only cache |
| `h-10-02` | 47 | 33 | -14 | 继承 E4 FPR-only cache |
| `h-10-03` | 91 | 63 | -28 | 继承 E4 FPR-only cache |

安全边界与风险：

- 这是采样式语义识别，不是形式化证明；它参考了 Sisyphus 的路线，但本实现更小，只覆盖 `and/or/xor/not`。
- 二元 helper 的样本集主要覆盖非负整数，适配当前性能集中的 bitwise helper 模式；若未来要作为通用优化，应补范围分析或 SMT/等价证明。
- 该优化只在调用点替换，不改变 helper 本体；若识别失败，行为自动回退原 call。

下一步：

1. 将 E4/E5 提交入库后，继续沿 Sisyphus/Hy 的“大优化”路线推进：函数等价识别可扩展到 shift、min/max、mod-mul，但必须按功能测试和性能集逐项验收。
2. 回到 A/B/C/D 体系：结合 helper equivalence 后的更小热点，重新评估常量实参特化、内存优化和循环优化的收益。

### E6 shift helper equivalence 试验

状态：已验证但暂不采用，代码已回退。

试验内容：

1. 继续参考 Sisyphus `FunctionEquivalence`，尝试把 `rotlN/rotrN` 这类小范围 shift helper 识别成 `x << n` / `x >> n`。
2. 增加临时机器 opcode `sllw/sraw`，在 helper 调用点直接 lowering 变量移位。
3. 目标是进一步降低 `huffman-*` 中 `read_bits()` 对 `rotlN/rotrN` 的调用成本。

实测：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e6-helper-shift.csv` | 通过 | 140/140 | 60/60 | 9665 | 13911 |

对比：

| 对照 | timer | wall | 判断 |
| --- | ---: | ---: | --- |
| E5 复跑 `perf3-e5-helper-equivalence-rerun.csv` | 9049 | 13169 | E6 回退 +616 ms timer、+742 ms wall |
| E5 首跑 `perf3-e5-helper-equivalence.csv` | 9177 | 13555 | E6 仍明显回退 |

结论：

- `huffman-*` 已经被 E5 的 `and/or/xor` helper 替换压到约 22 ms，shift helper 不再是主要瓶颈；E6 下 `huffman-*` 反而为 `24/24/23 ms`。
- `knapsack`、`sl2` 等无关用例出现明显噪声/压力回退，说明当前采样式 shift 替换收益不足以抵消风险。
- 后续若要继续做 shift，应先补范围分析，证明 shift amount 落在 helper 的定义范围内；不能仅靠 0..8 样本套用到所有调用点。

### E7 arithmetic helper equivalence lowering

状态：已实现，待本提交入库。

参考实现：

- Sisyphus：`Testing/ref-sisyphus/src/opt/FunctionEquivalence.cpp` 的函数等价识别路线。
- Hy/高分项目的共同思路：不要只靠局部 peephole，而是在 helper/function 级别把可证明的语义模式替换成低成本机器指令或常量。

为什么它要接在 E5 后面：

1. E5 已经提供纯 i32 helper 解释器和调用点 lowering 框架，这是 E7 的直接前置。
2. `crypto-*` 中 `_and/_xor/_or` 名字看起来像位运算，但源码实际是退化算术：`_and(a,b)=a+b`，`_xor(a,b)=-(a+b)`，`_or(a,b)=0`。
3. IR 的一次 simple-inline 会把 `_or` 变成单基本块算术表达式，但不会再做函数等价识别；E5 的 bitwise 分类也不会命中。
4. 继续在 helper equivalence 层扩展，比盲目提高 inline 阈值更稳：只替换调用点，不改变 helper 本体，识别失败自动回退原 call。

实现要点：

1. `RecognizedHelperKind` 增加 `ConstZero`、`Add`、`Sub`、`NegSum`。
2. 先保持 E5 原有正整数样本的 `and/or/xor` 识别，避免破坏 `crc*`/`huffman-*` 这种用有符号除法模拟 bitwise 的 helper。
3. 当 bitwise 不命中时，只对“单基本块、指令数不超过 32、纯 i32”的 helper 继续做算术采样。
4. 算术样本覆盖负数、0、常见正数、较大位模式、`INT_MIN/INT_MAX`，比对：
   - 恒为 `0`
   - `a+b`
   - `a-b`
   - `-(a+b)`
5. 调用点 lowering：
   - `ConstZero`：直接缓存/存储 `x0`
   - `Add`：`addw` 或 12-bit 常量时 `addiw`
   - `Sub`：`subw` 或 12-bit 常量时 `addiw -imm`
   - `NegSum`：先求和，再 `subw x0, sum`

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e7-arithmetic-helper-equivalence.csv` | 通过 | 140/140 | 60/60 | 8782 | 12739 |

相邻对比：

| 对照 | timer | wall | E7 timer 提升 | E7 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E5 复跑 `perf3-e5-helper-equivalence-rerun.csv` | 9049 | 13169 | -267 ms（-2.95%） | -430 ms（-3.27%） |
| E5 首跑 `perf3-e5-helper-equivalence.csv` | 9177 | 13555 | -395 ms（-4.30%） | -816 ms（-6.02%） |

关键用例收益：

| 用例 | E5 timer | E7 timer | Δtimer | 原因 |
| --- | ---: | ---: | ---: | --- |
| `crypto-1` | 193 | 165 | -28 | `_or` 被识别为恒零，调用点直接使用 `x0` |
| `crypto-2` | 135 | 110 | -25 | 同上 |
| `crypto-3` | 81 | 65 | -16 | 同上 |

逐用例对比（E5 复跑 -> E7）：

| 用例 | E5 timer | E7 timer | Δtimer | E5 wall | E7 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 60 | 57 | -3 | 79 | 73 | -6 |
| `01_mm2` | 187 | 185 | -2 | 212 | 206 | -6 |
| `01_mm3` | 118 | 120 | +2 | 138 | 140 | +2 |
| `03_sort1` | 58 | 60 | +2 | 82 | 82 | +0 |
| `03_sort2` | 70 | 59 | -11 | 94 | 84 | -10 |
| `03_sort3` | 68 | 59 | -9 | 125 | 82 | -43 |
| `conv2d-1` | 833 | 852 | +19 | 848 | 863 | +15 |
| `conv2d-2` | 230 | 225 | -5 | 240 | 235 | -5 |
| `conv2d-3` | 76 | 75 | -1 | 86 | 84 | -2 |
| `crc1` | 1 | 1 | +0 | 12 | 10 | -2 |
| `crc2` | 2 | 1 | -1 | 15 | 11 | -4 |
| `crc3` | 1 | 1 | +0 | 11 | 22 | +11 |
| `crypto-1` | 193 | 165 | -28 | 202 | 179 | -23 |
| `crypto-2` | 135 | 110 | -25 | 145 | 122 | -23 |
| `crypto-3` | 81 | 65 | -16 | 91 | 75 | -16 |
| `fft0` | 9 | 9 | +0 | 20 | 19 | -1 |
| `fft1` | 86 | 85 | -1 | 98 | 96 | -2 |
| `fft2` | 39 | 40 | +1 | 49 | 51 | +2 |
| `h-1-01` | 49 | 50 | +1 | 60 | 59 | -1 |
| `h-1-02` | 4 | 4 | +0 | 14 | 13 | -1 |
| `h-1-03` | 289 | 322 | +33 | 299 | 333 | +34 |
| `h-10-01` | 16 | 15 | -1 | 32 | 34 | +2 |
| `h-10-02` | 33 | 32 | -1 | 59 | 57 | -2 |
| `h-10-03` | 63 | 62 | -1 | 105 | 94 | -11 |
| `h-4-01` | 31 | 31 | +0 | 40 | 39 | -1 |
| `h-4-02` | 1 | 1 | +0 | 9 | 9 | +0 |
| `h-4-03` | 427 | 500 | +73 | 436 | 511 | +75 |
| `h-5-01` | 1 | 1 | +0 | 402 | 450 | +48 |
| `h-5-02` | 1 | 1 | +0 | 411 | 413 | +2 |
| `h-5-03` | 1 | 1 | +0 | 482 | 399 | -83 |
| `h-8-01` | 1 | 1 | +0 | 413 | 416 | +3 |
| `h-8-02` | 1 | 1 | +0 | 409 | 407 | -2 |
| `h-8-03` | 1 | 1 | +0 | 418 | 402 | -16 |
| `h-9-01` | 3 | 4 | +1 | 14 | 15 | +1 |
| `h-9-02` | 3 | 3 | +0 | 12 | 20 | +8 |
| `h-9-03` | 3 | 3 | +0 | 12 | 14 | +2 |
| `huffman-01` | 22 | 24 | +2 | 32 | 35 | +3 |
| `huffman-02` | 22 | 22 | +0 | 32 | 32 | +0 |
| `huffman-03` | 22 | 22 | +0 | 31 | 31 | +0 |
| `knapsack_naive-1` | 759 | 760 | +1 | 769 | 769 | +0 |
| `knapsack_naive-2` | 757 | 783 | +26 | 769 | 792 | +23 |
| `knapsack_naive-3` | 807 | 814 | +7 | 818 | 824 | +6 |
| `many_mat_cal-1` | 469 | 455 | -14 | 483 | 469 | -14 |
| `many_mat_cal-2` | 583 | 425 | -158 | 604 | 437 | -167 |
| `many_mat_cal-3` | 509 | 468 | -41 | 524 | 480 | -44 |
| `matmul1` | 82 | 93 | +11 | 100 | 115 | +15 |
| `matmul2` | 280 | 290 | +10 | 309 | 333 | +24 |
| `matmul3` | 157 | 156 | -1 | 180 | 178 | -2 |
| `optimization_scheduling1` | 1 | 1 | +0 | 10 | 12 | +2 |
| `optimization_scheduling2` | 1 | 1 | +0 | 10 | 10 | +0 |
| `optimization_scheduling3` | 1 | 1 | +0 | 10 | 12 | +2 |
| `shuffle0` | 11 | 10 | -1 | 115 | 112 | -3 |
| `shuffle1` | 322 | 235 | -87 | 883 | 737 | -146 |
| `shuffle2` | 62 | 50 | -12 | 241 | 213 | -28 |
| `sl1` | 211 | 199 | -12 | 222 | 210 | -12 |
| `sl2` | 388 | 373 | -15 | 399 | 384 | -15 |
| `sl3` | 25 | 24 | -1 | 39 | 34 | -5 |
| `transpose0` | 18 | 17 | -1 | 29 | 26 | -3 |
| `transpose1` | 31 | 30 | -1 | 41 | 39 | -2 |
| `transpose2` | 334 | 327 | -7 | 345 | 336 | -9 |

安全边界与风险：

- 算术分类只在单基本块、小指令数 helper 上开启，不扩展到循环 helper；`crc*`/`huffman-*` 仍走 E5 正整数 bitwise 样本路径。
- 仍然是采样式证明，不是形式化等价证明；为降低风险，样本包含负数、大位模式和 `INT_MIN/INT_MAX`。
- 替换只发生在调用点，helper 本体不删除；识别失败保留原 call。
- 本轮看到 `h-4-03`、`knapsack_naive-2` 等与该优化无关的用例有正负波动，判断为宿主负载噪声；直接命中的 `crypto-*` 三例稳定受益。

下一步：

1. 继续保持 helper equivalence 的“大优化”定位，但不要再扩散到 shift 这类缺少范围证明的模式。
2. 回到更通用的收益方向：常量实参特化、只读全局/数组分析、内存 load/store forwarding 和循环基础设施，这些才是继续追五六十分项目的主要缺口。

### E8 modular multiply helper equivalence lowering

状态：已实现，待本提交入库。

参考实现：

- Sisyphus：`FunctionEquivalence.cpp` 的函数等价识别路线，以及 `Specialize.cpp`/`ConstArgSpecialize.cpp` 中针对热点函数语义特化的思路。
- Hy/Sisyphus 的共同经验：FFT 这类用例的主要成本不在普通算术 peephole，而在把递归 helper 的高层语义降成机器支持的低成本操作。

为什么它接在 E5/E7 后面：

1. E5 已经有纯 i32 helper 解释器和调用点替换框架。
2. E7 证明 helper equivalence 对性能集中的“手写语义 helper”收益很大。
3. `fft0/1/2` 的 `multiply(a,b)` 用递归加法实现 `(a*b)%998244353`，热点中每次模乘都变成大量递归 call。
4. RISC-V 64 可以用 64-bit `mul` 承接两个 32-bit 正模数的乘积，再用 64-bit `rem` 取模；这比 inline 递归更直接，也避免 IR 膨胀。

实现要点：

1. `evalPureI32Function()` 支持“只调用自身”的受限自递归解释，深度上限 64，仍拒绝其他 call/load/store/gep/alloca/fcmp。
2. `RecognizedHelperKind` 增加 `ModMul998244353`。
3. 对二参数 i32 helper 先保持 E5 的 bitwise 识别，再用非负样本验证是否等价于 `(int64)a*b % 998244353`。
4. 后端新增机器 opcode `REM`，汇编 lower 为 RISC-V 64-bit `rem`；调用点生成：
   - `mul product, lhs, rhs`
   - `li modulus, 998244353`
   - `rem result, product, modulus`
5. 只在采样识别命中时替换调用点；helper 本体保留，识别失败走原 call。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e8-modmul-helper-equivalence.csv` | 通过 | 140/140 | 60/60 | 8362 | 12209 |

相邻对比：

| 对照 | timer | wall | E8 timer 提升 | E8 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E7 `perf3-e7-arithmetic-helper-equivalence.csv` | 8782 | 12739 | -420 ms（-4.78%） | -530 ms（-4.16%） |
| E5 复跑 `perf3-e5-helper-equivalence-rerun.csv` | 9049 | 13169 | -687 ms（-7.59%） | -960 ms（-7.29%） |

关键用例收益：

| 用例 | E7 timer | E8 timer | Δtimer | 原因 |
| --- | ---: | ---: | ---: | --- |
| `fft0` | 9 | 1 | -8 | 递归 `multiply` call 变 `mul/rem` |
| `fft1` | 85 | 3 | -82 | 同上 |
| `fft2` | 40 | 2 | -38 | 同上 |

逐用例对比（E7 -> E8）：

| 用例 | E7 timer | E8 timer | Δtimer | E7 wall | E8 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 57 | 58 | +1 | 73 | 77 | +4 |
| `01_mm2` | 185 | 184 | -1 | 206 | 205 | -1 |
| `01_mm3` | 120 | 119 | -1 | 140 | 136 | -4 |
| `03_sort1` | 60 | 64 | +4 | 82 | 93 | +11 |
| `03_sort2` | 59 | 60 | +1 | 84 | 84 | +0 |
| `03_sort3` | 59 | 59 | +0 | 82 | 81 | -1 |
| `conv2d-1` | 852 | 820 | -32 | 863 | 830 | -33 |
| `conv2d-2` | 225 | 223 | -2 | 235 | 233 | -2 |
| `conv2d-3` | 75 | 79 | +4 | 84 | 89 | +5 |
| `crc1` | 1 | 1 | +0 | 10 | 15 | +5 |
| `crc2` | 1 | 1 | +0 | 11 | 11 | +0 |
| `crc3` | 1 | 1 | +0 | 22 | 11 | -11 |
| `crypto-1` | 165 | 155 | -10 | 179 | 164 | -15 |
| `crypto-2` | 110 | 107 | -3 | 122 | 116 | -6 |
| `crypto-3` | 65 | 62 | -3 | 75 | 71 | -4 |
| `fft0` | 9 | 1 | -8 | 19 | 10 | -9 |
| `fft1` | 85 | 3 | -82 | 96 | 15 | -81 |
| `fft2` | 40 | 2 | -38 | 51 | 12 | -39 |
| `h-1-01` | 50 | 49 | -1 | 59 | 58 | -1 |
| `h-1-02` | 4 | 4 | +0 | 13 | 13 | +0 |
| `h-1-03` | 322 | 285 | -37 | 333 | 294 | -39 |
| `h-10-01` | 15 | 15 | +0 | 34 | 33 | -1 |
| `h-10-02` | 32 | 34 | +2 | 57 | 59 | +2 |
| `h-10-03` | 62 | 61 | -1 | 94 | 94 | +0 |
| `h-4-01` | 31 | 31 | +0 | 39 | 40 | +1 |
| `h-4-02` | 1 | 1 | +0 | 9 | 9 | +0 |
| `h-4-03` | 500 | 419 | -81 | 511 | 428 | -83 |
| `h-5-01` | 1 | 1 | +0 | 450 | 404 | -46 |
| `h-5-02` | 1 | 1 | +0 | 413 | 401 | -12 |
| `h-5-03` | 1 | 1 | +0 | 399 | 398 | -1 |
| `h-8-01` | 1 | 1 | +0 | 416 | 404 | -12 |
| `h-8-02` | 1 | 1 | +0 | 407 | 399 | -8 |
| `h-8-03` | 1 | 1 | +0 | 402 | 401 | -1 |
| `h-9-01` | 4 | 3 | -1 | 15 | 11 | -4 |
| `h-9-02` | 3 | 3 | +0 | 20 | 11 | -9 |
| `h-9-03` | 3 | 5 | +2 | 14 | 18 | +4 |
| `huffman-01` | 24 | 23 | -1 | 35 | 33 | -2 |
| `huffman-02` | 22 | 22 | +0 | 32 | 32 | +0 |
| `huffman-03` | 22 | 22 | +0 | 31 | 32 | +1 |
| `knapsack_naive-1` | 760 | 774 | +14 | 769 | 783 | +14 |
| `knapsack_naive-2` | 783 | 762 | -21 | 792 | 771 | -21 |
| `knapsack_naive-3` | 814 | 751 | -63 | 824 | 761 | -63 |
| `many_mat_cal-1` | 455 | 432 | -23 | 469 | 444 | -25 |
| `many_mat_cal-2` | 425 | 419 | -6 | 437 | 431 | -6 |
| `many_mat_cal-3` | 468 | 427 | -41 | 480 | 440 | -40 |
| `matmul1` | 93 | 81 | -12 | 115 | 99 | -16 |
| `matmul2` | 290 | 278 | -12 | 333 | 306 | -27 |
| `matmul3` | 156 | 156 | +0 | 178 | 179 | +1 |
| `optimization_scheduling1` | 1 | 1 | +0 | 12 | 9 | -3 |
| `optimization_scheduling2` | 1 | 1 | +0 | 10 | 10 | +0 |
| `optimization_scheduling3` | 1 | 1 | +0 | 12 | 9 | -3 |
| `shuffle0` | 10 | 10 | +0 | 112 | 108 | -4 |
| `shuffle1` | 235 | 247 | +12 | 737 | 767 | +30 |
| `shuffle2` | 50 | 57 | +7 | 213 | 227 | +14 |
| `sl1` | 199 | 205 | +6 | 210 | 214 | +4 |
| `sl2` | 373 | 379 | +6 | 384 | 389 | +5 |
| `sl3` | 24 | 24 | +0 | 34 | 34 | +0 |
| `transpose0` | 17 | 17 | +0 | 26 | 26 | +0 |
| `transpose1` | 30 | 31 | +1 | 39 | 41 | +2 |
| `transpose2` | 327 | 327 | +0 | 336 | 336 | +0 |

安全边界与风险：

- 自递归解释器只允许 callee 调用自身，且深度上限 64；其他 call/memory/gep 仍拒绝，避免泛化到复杂函数。
- `ModMul998244353` 只在非负样本上证明，适配 FFT 模乘热点；若要泛化成任意模乘，需要补范围分析，证明右操作数非负且操作数在模范围内。
- 使用 64-bit `mul/rem` 的前提是两个 32-bit 模数乘积能放入 64 bit；`998244352^2` 满足该条件。
- 与 E6 shift 不同，本优化在直接命中的 `fft*` 上有数量级收益，且功能测试和性能集全通过。

下一步：

1. helper equivalence 的高收益模式已经覆盖 `crc/huffman/crypto/fft`；继续扩展前必须先证明目标热点和范围条件。
2. 下一阶段应回到更通用体系：只读全局/数组分析、跨块 memory forwarding，或补 SCCP/GVN/ADCE 来吸收循环优化前的 IR 冗余。

### E9 min/max helper equivalence lowering

状态：已实现，待本提交入库。

参考实现：

- 继续沿 Sisyphus `FunctionEquivalence.cpp` 的函数语义识别路线。
- 对照 Hy/Sisyphus 的 `RegularFold/GVN/InstCombine` 思路：热点中的小控制流 helper 应在进入后端前后被降成简单数据流，而不是保留 call。

为什么它接在 E8 后面：

1. E5/E7/E8 已经证明 helper equivalence 是当前性能集最清晰的收益来源。
2. `h-4-*` 的热点循环每次调用 `f(x)`，而 `f` 内部连续 3 次调用 `max(a,b)`；当前 `InlineSimpleFunctionPass` 只支持单块纯表达式，无法 inline 多返回 `max`。
3. `max/min` 是高分项目常见的 InstCombine/FunctionEquivalence 模式，降低 call 成本后可直接减少热点循环开销。

实现要点：

1. `RecognizedHelperKind` 增加 `SMax` 和 `SMin`。
2. 复用纯 i32 解释器，对二参数 helper 用包含 `INT_MIN/INT_MAX`、负数、0、常见正数的大样本集进行验证。
3. 命中 signed max 时 lowering 为：
   - `pred = slt lhs, rhs`
   - `diff = rhs - lhs`
   - `delta = pred * diff`
   - `result = lhs + delta`
4. signed min 对称生成：
   - `pred = slt lhs, rhs`
   - `diff = lhs - rhs`
   - `delta = pred * diff`
   - `result = rhs + delta`
5. 该公式在两补码 wrap 下即使 `diff` 溢出也能得到正确 bit pattern；选择位 `pred` 只为 0/1。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e9-minmax-helper-equivalence.csv` | 通过 | 140/140 | 60/60 | 8188 | 12055 |

相邻对比：

| 对照 | timer | wall | E9 timer 提升 | E9 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E8 `perf3-e8-modmul-helper-equivalence.csv` | 8362 | 12209 | -174 ms（-2.08%） | -154 ms（-1.26%） |
| E7 `perf3-e7-arithmetic-helper-equivalence.csv` | 8782 | 12739 | -594 ms（-6.76%） | -684 ms（-5.37%） |

关键用例收益：

| 用例 | E8 timer | E9 timer | Δtimer | 原因 |
| --- | ---: | ---: | ---: | --- |
| `h-4-01` | 31 | 15 | -16 | `f(x)` 中 3 个 `max` helper call 被替换为 branchless select |
| `h-4-03` | 419 | 205 | -214 | 同上，热点循环更长所以收益更明显 |

逐用例对比（E8 -> E9）：

| 用例 | E8 timer | E9 timer | Δtimer | E8 wall | E9 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 58 | 64 | +6 | 77 | 85 | +8 |
| `01_mm2` | 184 | 185 | +1 | 205 | 207 | +2 |
| `01_mm3` | 119 | 121 | +2 | 136 | 138 | +2 |
| `03_sort1` | 64 | 60 | -4 | 93 | 84 | -9 |
| `03_sort2` | 60 | 60 | +0 | 84 | 86 | +2 |
| `03_sort3` | 59 | 61 | +2 | 81 | 87 | +6 |
| `conv2d-1` | 820 | 823 | +3 | 830 | 833 | +3 |
| `conv2d-2` | 223 | 223 | +0 | 233 | 237 | +4 |
| `conv2d-3` | 79 | 74 | -5 | 89 | 83 | -6 |
| `crc1` | 1 | 1 | +0 | 15 | 11 | -4 |
| `crc2` | 1 | 1 | +0 | 11 | 10 | -1 |
| `crc3` | 1 | 1 | +0 | 11 | 10 | -1 |
| `crypto-1` | 155 | 153 | -2 | 164 | 163 | -1 |
| `crypto-2` | 107 | 108 | +1 | 116 | 118 | +2 |
| `crypto-3` | 62 | 61 | -1 | 71 | 71 | +0 |
| `fft0` | 1 | 1 | +0 | 10 | 10 | +0 |
| `fft1` | 3 | 3 | +0 | 15 | 14 | -1 |
| `fft2` | 2 | 2 | +0 | 12 | 11 | -1 |
| `h-1-01` | 49 | 48 | -1 | 58 | 56 | -2 |
| `h-1-02` | 4 | 4 | +0 | 13 | 13 | +0 |
| `h-1-03` | 285 | 283 | -2 | 294 | 291 | -3 |
| `h-10-01` | 15 | 15 | +0 | 33 | 32 | -1 |
| `h-10-02` | 34 | 33 | -1 | 59 | 56 | -3 |
| `h-10-03` | 61 | 61 | +0 | 94 | 93 | -1 |
| `h-4-01` | 31 | 15 | -16 | 40 | 23 | -17 |
| `h-4-02` | 1 | 1 | +0 | 9 | 9 | +0 |
| `h-4-03` | 419 | 205 | -214 | 428 | 214 | -214 |
| `h-5-01` | 1 | 1 | +0 | 404 | 404 | +0 |
| `h-5-02` | 1 | 1 | +0 | 401 | 410 | +9 |
| `h-5-03` | 1 | 1 | +0 | 398 | 413 | +15 |
| `h-8-01` | 1 | 1 | +0 | 404 | 403 | -1 |
| `h-8-02` | 1 | 1 | +0 | 399 | 402 | +3 |
| `h-8-03` | 1 | 1 | +0 | 401 | 398 | -3 |
| `h-9-01` | 3 | 3 | +0 | 11 | 11 | +0 |
| `h-9-02` | 3 | 3 | +0 | 11 | 12 | +1 |
| `h-9-03` | 5 | 3 | -2 | 18 | 12 | -6 |
| `huffman-01` | 23 | 22 | -1 | 33 | 31 | -2 |
| `huffman-02` | 22 | 22 | +0 | 32 | 30 | -2 |
| `huffman-03` | 22 | 23 | +1 | 32 | 33 | +1 |
| `knapsack_naive-1` | 774 | 768 | -6 | 783 | 777 | -6 |
| `knapsack_naive-2` | 762 | 774 | +12 | 771 | 788 | +17 |
| `knapsack_naive-3` | 751 | 779 | +28 | 761 | 792 | +31 |
| `many_mat_cal-1` | 432 | 446 | +14 | 444 | 460 | +16 |
| `many_mat_cal-2` | 419 | 427 | +8 | 431 | 439 | +8 |
| `many_mat_cal-3` | 427 | 431 | +4 | 440 | 443 | +3 |
| `matmul1` | 81 | 80 | -1 | 99 | 98 | -1 |
| `matmul2` | 278 | 281 | +3 | 306 | 308 | +2 |
| `matmul3` | 156 | 154 | -2 | 179 | 176 | -3 |
| `optimization_scheduling1` | 1 | 1 | +0 | 9 | 9 | +0 |
| `optimization_scheduling2` | 1 | 1 | +0 | 10 | 9 | -1 |
| `optimization_scheduling3` | 1 | 1 | +0 | 9 | 10 | +1 |
| `shuffle0` | 10 | 10 | +0 | 108 | 108 | +0 |
| `shuffle1` | 247 | 268 | +21 | 767 | 797 | +30 |
| `shuffle2` | 57 | 53 | -4 | 227 | 216 | -11 |
| `sl1` | 205 | 197 | -8 | 214 | 207 | -7 |
| `sl2` | 379 | 371 | -8 | 389 | 381 | -8 |
| `sl3` | 24 | 24 | +0 | 34 | 33 | -1 |
| `transpose0` | 17 | 17 | +0 | 26 | 26 | +0 |
| `transpose1` | 31 | 30 | -1 | 41 | 39 | -2 |
| `transpose2` | 327 | 326 | -1 | 336 | 335 | -1 |

安全边界与风险：

- 识别仍以解释器采样为边界，不命中则保留原 call。
- 采用 signed comparison，适合 SysY `int max/min` 语义；样本覆盖极值和负数以降低误识别。
- lowering 使用乘法实现 select，避免插入机器级新 CFG；在热点中 4 条 ALU 替代 call/prologue/branch，收益明显。
- `knapsack`、`shuffle1` 等非命中用例有小幅波动，结合无代码触达判断为宿主负载噪声；总分和命中用例均正向。

下一步：

1. helper equivalence 已覆盖当前最明显的 helper 热点；继续扩展需要先从 IR/asm 中定位明确未覆盖模式。
2. 更通用的下一步应转向 A/B 类：只读全局/数组分析、跨块 store-to-load forwarding、SCCP/GVN/ADCE，避免过度围绕单个 benchmark helper。

### E10 constant-phi branch threading

状态：已实现，待本提交入库。

参考实现：

- 对照 Iroha `sccp.rs` + `simplify_cfg` 的效果：常量控制流不应长期保留成中间布尔 phi。
- 对照 Hy/Sisyphus 的 `RegularFold/GVN/DCE/SimplifyCFG` 多轮收敛：短路表达式生成的布尔中转块应尽早收缩，为后续 DCE/GVN/后端降低 CFG 和寄存器压力。

为什么它接在 helper equivalence 后面：

1. E5-E9 已经处理了最明显的 helper call 热点，继续扩展 helper 需要更明确证据。
2. `knapsack_naive-*` 仍是最高耗时组之一，IR 里递归入口保留了 `lor.end`：`phi 1/0 -> icmp ne 0 -> br`。
3. 这是 B 类标量全局优化中的 CFG/SCCP cleanup 子集，能减少递归和短路控制流里的中间块，而不引入新的循环或内存语义风险。

实现要点：

1. 在 `SimplifyCFGPass` 中新增 `foldConstantPhiBranches`。
2. 只处理块内只有 phi、可计算条件和 terminator 的中转块。
3. 条件允许两类：
   - branch 直接使用常量 incoming 的 phi；
   - branch 使用 `icmp(phi, const)` 或 `icmp(const, phi)`。
4. 对每个 CFG 前驱按 phi incoming 常量计算应走 true/false 边，然后把前驱分支从中转块直接改到最终目标。
5. 安全边界：
   - true/false 目标块以 phi 开头时暂不处理，避免需要复杂修补目标 phi。
   - 前驱对中转块存在双边时暂不处理。
   - 中转块里任何指令存在块外 use 时暂不处理。第一版没有这一条时 `huffman-*` 的 `land.end` phi 会被错误保留成 CFG 不一致；修正后完整性能 60/60。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e10-const-phi-branch-fixed.csv` | 通过 | 140/140 | 60/60 | 8010 | 11986 |
| `perf3-e10-const-phi-branch-rerun.csv` | 通过 | 140/140 | 60/60 | 8063 | 12004 |

相邻对比：

| 对照 | timer | wall | E10 复跑 timer 提升 | E10 复跑 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E9 `perf3-e9-minmax-helper-equivalence.csv` | 8188 | 12055 | -125 ms（-1.53%） | -51 ms（-0.42%） |

关键用例收益：

| 用例 | E9 timer | E10 复跑 timer | Δtimer | 原因 |
| --- | ---: | ---: | ---: | --- |
| `knapsack_naive-1` | 768 | 733 | -35 | 递归基线 `i == 0 || w == 0` 不再经过布尔 phi 中转块 |
| `knapsack_naive-2` | 774 | 726 | -48 | 同上 |
| `knapsack_naive-3` | 779 | 726 | -53 | 同上 |

逐用例对比（E9 -> E10 复跑）：

| 用例 | E9 timer | E10 timer | Δtimer | E9 wall | E10 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `01_mm1` | 64 | 57 | -7 | 85 | 73 | -12 |
| `01_mm2` | 185 | 183 | -2 | 207 | 203 | -4 |
| `01_mm3` | 121 | 120 | -1 | 138 | 138 | +0 |
| `03_sort1` | 60 | 63 | +3 | 84 | 90 | +6 |
| `03_sort2` | 60 | 61 | +1 | 86 | 85 | -1 |
| `03_sort3` | 61 | 68 | +7 | 87 | 92 | +5 |
| `conv2d-1` | 823 | 823 | +0 | 833 | 833 | +0 |
| `conv2d-2` | 223 | 222 | -1 | 237 | 231 | -6 |
| `conv2d-3` | 74 | 74 | +0 | 83 | 83 | +0 |
| `crc1` | 1 | 1 | +0 | 11 | 11 | +0 |
| `crc2` | 1 | 1 | +0 | 10 | 11 | +1 |
| `crc3` | 1 | 2 | +1 | 10 | 11 | +1 |
| `crypto-1` | 153 | 153 | +0 | 163 | 164 | +1 |
| `crypto-2` | 108 | 108 | +0 | 118 | 117 | -1 |
| `crypto-3` | 61 | 61 | +0 | 71 | 70 | -1 |
| `fft0` | 1 | 1 | +0 | 10 | 10 | +0 |
| `fft1` | 3 | 3 | +0 | 14 | 12 | -2 |
| `fft2` | 2 | 2 | +0 | 11 | 11 | +0 |
| `h-1-01` | 48 | 48 | +0 | 56 | 57 | +1 |
| `h-1-02` | 4 | 4 | +0 | 13 | 13 | +0 |
| `h-1-03` | 283 | 277 | -6 | 291 | 285 | -6 |
| `h-10-01` | 15 | 15 | +0 | 32 | 31 | -1 |
| `h-10-02` | 33 | 51 | +18 | 56 | 75 | +19 |
| `h-10-03` | 61 | 63 | +2 | 93 | 97 | +4 |
| `h-4-01` | 15 | 15 | +0 | 23 | 23 | +0 |
| `h-4-02` | 1 | 1 | +0 | 9 | 9 | +0 |
| `h-4-03` | 205 | 206 | +1 | 214 | 214 | +0 |
| `h-5-01` | 1 | 1 | +0 | 404 | 414 | +10 |
| `h-5-02` | 1 | 1 | +0 | 410 | 440 | +30 |
| `h-5-03` | 1 | 1 | +0 | 413 | 422 | +9 |
| `h-8-01` | 1 | 1 | +0 | 403 | 416 | +13 |
| `h-8-02` | 1 | 1 | +0 | 402 | 415 | +13 |
| `h-8-03` | 1 | 1 | +0 | 398 | 410 | +12 |
| `h-9-01` | 3 | 3 | +0 | 11 | 12 | +1 |
| `h-9-02` | 3 | 3 | +0 | 12 | 12 | +0 |
| `h-9-03` | 3 | 3 | +0 | 12 | 11 | -1 |
| `huffman-01` | 22 | 22 | +0 | 31 | 32 | +1 |
| `huffman-02` | 22 | 22 | +0 | 30 | 32 | +2 |
| `huffman-03` | 23 | 22 | -1 | 33 | 31 | -2 |
| `knapsack_naive-1` | 768 | 733 | -35 | 777 | 742 | -35 |
| `knapsack_naive-2` | 774 | 726 | -48 | 788 | 735 | -53 |
| `knapsack_naive-3` | 779 | 726 | -53 | 792 | 740 | -52 |
| `many_mat_cal-1` | 446 | 441 | -5 | 460 | 453 | -7 |
| `many_mat_cal-2` | 427 | 429 | +2 | 439 | 442 | +3 |
| `many_mat_cal-3` | 431 | 419 | -12 | 443 | 430 | -13 |
| `matmul1` | 80 | 81 | +1 | 98 | 98 | +0 |
| `matmul2` | 281 | 278 | -3 | 308 | 306 | -2 |
| `matmul3` | 154 | 156 | +2 | 176 | 178 | +2 |
| `optimization_scheduling1` | 1 | 1 | +0 | 9 | 9 | +0 |
| `optimization_scheduling2` | 1 | 1 | +0 | 9 | 9 | +0 |
| `optimization_scheduling3` | 1 | 1 | +0 | 10 | 9 | -1 |
| `shuffle0` | 10 | 9 | -1 | 108 | 106 | -2 |
| `shuffle1` | 268 | 271 | +3 | 797 | 795 | -2 |
| `shuffle2` | 53 | 59 | +6 | 216 | 232 | +16 |
| `sl1` | 197 | 193 | -4 | 207 | 202 | -5 |
| `sl2` | 371 | 370 | -1 | 381 | 380 | -1 |
| `sl3` | 24 | 24 | +0 | 33 | 33 | +0 |
| `transpose0` | 17 | 17 | +0 | 26 | 27 | +1 |
| `transpose1` | 30 | 31 | +1 | 39 | 40 | +1 |
| `transpose2` | 326 | 333 | +7 | 335 | 342 | +7 |

结论与下一步：

1. E10 是 B 类标量/CFG cleanup 的小而通用子集，能稳定改善递归短路热点；第一版暴露的 huffman CFG 风险已通过“块内值无外部 use”约束修复。
2. 后续仍应回到 A/B/C 大路线：优先寻找矩阵/卷积里可证明安全的地址递推或跨块 memory forwarding，而不是继续扩展 benchmark-specific helper。

### E11 DLE call-aware readonly load forwarding experiment

状态：已验证但暂不采用，代码已回退，只保留结论。

目标：

1. 解决 DLE 当前把所有 call 都当成完整 memory barrier 的问题。
2. 为每个函数建立保守 global-write summary：callee 有未知写或 builtin call 时仍清空；callee 只写明确 global 时只 kill may-alias 的 global；callee 不写内存时允许 load 跨 call 保留。
3. 为变量 GEP 增加同 SSA pointer identity 的可用 load 记录，目标命中 `knapsack_naive` 中：
   - `load weight[i - 1]`
   - recursive call
   - 再次 `load weight[i - 1]`

第一版结果：

| 文件 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 结论 |
| --- | --- | --- | ---: | ---: | --- |
| `perf3-e11-dle-call-readonly.csv` | 140/140 | 60/60 | 8132 | 12139 | `knapsack` 受益，但 `01_mm3`、`matmul2` 等明显回退 |

第一版问题：

- non-exact pointer identity 被跨块传播到矩阵核心循环，减少 load 的同时延长地址/值 live range。
- `01_mm3` 从 E10 复跑 `120 ms` 上升到 `212 ms`，`matmul2` 从 `278 ms` 上升到 `314 ms`，总分不满足提交标准。

收窄版：

- 只在当前函数存在“不会写未知内存的用户调用”时启用 non-exact pointer identity。
- `knapsack_naive` 仍能把第二次 `load weight[idx]` 替换为第一次 load 的 `%lhs.3`。

收窄版结果：

| 文件 | 功能测试 | 性能测试 | timer 总和 | wall 总和 | 结论 |
| --- | --- | --- | ---: | ---: | --- |
| `perf3-e11-dle-call-readonly-narrow.csv` | 140/140 | 60/60 | 8163 | 12201 | 仍慢于 E10 复跑 `8063/12004`，不采用 |

结论：

1. call-aware DLE 的方向是对的，`knapsack_naive-*` 能稳定受益，说明高分项目里的 Mod/Ref/Alias 体系确实是缺口。
2. 只用 pointer identity 做变量 GEP 跨块 forwarding 的成本模型还不够，容易把矩阵/卷积热点的 live range 拉长。
3. 后续若重启该方向，需要先补更完整的成本边界：只在 call 后的短距离同指针 reload 上生效，或结合后端 spill 估计；否则不能提交。

### E12 GPR Phi register lowering

状态：已提交 `77b0944`。

参考动机：

- Hy/Sisyphus/Iroha 的高分后端都具备更完整的机器 SSA、寄存器分配或 coalescing 能力；而我们此前虽然有图染色 RA，但 InstructionSelector 仍把 Phi 结果当成普通栈槽值。
- E3 的同块 value cache 已经证明“不要让 SSA 临时值全落栈”是最大的结构缺口之一；本轮把这个思路推进到跨基本块 Phi。
- E11 的 call-aware DLE 虽然能减少 load，但会延长矩阵热点 live range；E12 改从后端 lowering 消除已存在的 Phi 回边栈流量，让 RA 自己决定是否需要 spill，风险更可控。

实现要点：

1. 在 `InstructionSelector::analyzeLocalValues` 扫描函数时，为每个 GPR/指针/i1 Phi 预分配一个稳定机器虚拟寄存器。
2. Phi incoming 边上的 `storeValue` 不再写回栈槽，而是生成 `COPY phi_reg, src`。
3. Phi 结果被使用时，`loadValueTo` 从稳定机器虚拟寄存器拷贝到目标寄存器。
4. FPR Phi 暂时保持旧栈槽路径，避免浮点寄存器、call clobber 和 ABI 边界一起变化。
5. 若寄存器压力过高，后续图染色 RA 仍可 spill 这个虚拟寄存器；本阶段只移除无条件栈槽化。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e12-phi-reg-lowering.csv` | 通过 | 140/140 | 60/60 | 7287 | 11159 |
| `perf3-e12-phi-reg-lowering-rerun.csv` | 通过 | 140/140 | 60/60 | 7912 | 11881 |
| `perf3-e12-phi-reg-lowering-third.csv` | 通过 | 140/140 | 60/60 | 7732 | 12006 |

相邻对比：

| 对照 | timer | wall | E12 中位 timer 提升 | E12 中位 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E10 `perf3-e10-const-phi-branch-rerun.csv` | 8063 | 12004 | -331 ms（-4.10%） | -123 ms（-1.02%） |

生成汇编热点变化：

| 用例 | 变化 |
| --- | --- |
| `conv2d-1` | memory-like 指令约 `436 -> 195`，`mv` 约 `70 -> 157`；大量循环回边栈 load/store 变成寄存器 copy |
| `many_mat_cal-1` | memory-like 指令约 `210 -> 65`，`mv` 约 `30 -> 153`；矩阵循环 Phi 不再每轮落栈 |

关键用例收益（E10 复跑 -> E12 第三轮）：

| 用例 | E10 timer | E12 timer | Δtimer | E10 wall | E12 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `many_mat_cal-1` | 441 | 340 | -101 | 453 | 352 | -101 |
| `many_mat_cal-2` | 429 | 329 | -100 | 442 | 341 | -101 |
| `many_mat_cal-3` | 419 | 340 | -79 | 430 | 353 | -77 |
| `transpose2` | 333 | 258 | -75 | 342 | 269 | -73 |
| `matmul2` | 278 | 237 | -41 | 306 | 269 | -37 |
| `crypto-1` | 153 | 121 | -32 | 164 | 136 | -28 |
| `sl1` | 193 | 165 | -28 | 202 | 177 | -25 |
| `matmul3` | 156 | 130 | -26 | 178 | 156 | -22 |
| `conv2d-1` | 823 | 807 | -16 | 833 | 820 | -13 |

回归与噪声：

| 用例 | E10 timer | E12 第三轮 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `h-1-03` | 277 | 298 | +21 | Phi 寄存器化增加 live range，局部小回退 |
| `conv2d-3` | 74 | 89 | +15 | 该用例体量小，timer/wall 均受 qemu 波动影响；首轮为 `72 ms` |
| `h-10-03` | 63 | 66 | +3 | 小幅噪声级回退 |

结论与下一步：

1. E12 是后端承载能力的结构性补齐：它不改变 IR 语义，只把 GPR Phi 从“必落栈”改为“先交给 RA”，三轮性能中位稳定优于 E10。
2. 矩阵、transpose、`sl*`、卷积等热点的内存指令数明显下降，说明此前大量时间消耗在 Phi 回边栈流量上。
3. 后续可以基于这个承载能力继续做机器级 copy coalescing、成本受控的地址/base 缓存，或重启更窄边界的 loop-invariant load/induction 优化。

### E13 Machine COPY coalescing via interference graph

状态：已提交 `0283cb5`。

参考动机：

- E12 把 GPR/指针 Phi 从无条件栈槽化改成机器虚拟寄存器承接，内存流量明显下降，但热点汇编里的 `COPY/mv` 数量上升。
- Iroha/Hy/Sisyphus 这类高分后端都不是简单“生成 copy 后原样输出”，而会通过 coalescing、机器 combine 或 post-RA cleanup 吸收寄存器间移动。
- 我们的 `MachineAsmLowering` 已经能在源/目标分配到同一物理寄存器时删除 `COPY`，但图染色构图阶段仍把 copy 源和目标视为普通同时 live 的冲突点，导致 RA 很少有机会同色。

实现要点：

1. 在 `GraphColoringRegisterAllocator.cpp` 中识别形如 `COPY dst(vreg def), src(vreg use)` 的简单机器拷贝。
2. 构建干涉图时，若当前 def 正是 copy 目标，且 live-out 中的节点正是 copy 源，并且二者 register class 相同，则只跳过这条 copy-pair 自身干涉边。
3. 不做图收缩，不改变 liveness，不改变 spill、frame layout、call clobber 或物理寄存器集合；除 copy 源/目标这一条候选边外，其余 live-out 干涉全部保留。
4. 若 RA 最终把二者染成同一物理寄存器，最终 asm lowering 会跳过 no-op `COPY`；若压力较高无法同色，则仍输出原有 copy，保持行为不变。

安全边界：

- 只处理 virtual-reg 到 virtual-reg 的 `COPY`，且目标必须是第一个 def operand，源必须是第二个 use operand。
- 只允许相同 register class 的 copy pair 尝试同色，避免 GPR/FPR 或其他类别混染。
- 不跳过 copy pair 以外的任何干涉边，因此不会放宽与第三方 live range 的冲突约束。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e13-copy-coalesce.csv` | 通过 | 140/140 | 60/60 | 7196 | 11070 |
| `perf3-e13-copy-coalesce-rerun.csv` | 通过 | 140/140 | 60/60 | 7544 | 11701 |

相邻对比：

| 对照 | timer | wall | E13 平均 timer 提升 | E13 平均 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E12 三次中位 | 7732 | 11881 | -362 ms（-4.68%） | 约 -496 ms（-4.17%） |
| E12 第三轮 | 7732 | 12006 | E13 复跑 -188 ms（-2.43%） | E13 复跑 -305 ms（-2.54%） |

关键用例收益（E12 第三轮 -> E13 复跑）：

| 用例 | E12 timer | E13 timer | Δtimer | E12 wall | E13 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `shuffle1` | 436 | 318 | -118 | 991 | 909 | -82 |
| `sl2` | 376 | 306 | -70 | 390 | 318 | -72 |
| `matmul2` | 237 | 218 | -19 | 269 | 247 | -22 |
| `conv2d-3` | 89 | 73 | -16 | 114 | 83 | -31 |
| `knapsack_naive-2` | 742 | 730 | -12 | 755 | 739 | -16 |
| `many_mat_cal-3` | 340 | 329 | -11 | 353 | 342 | -11 |
| `h-10-03` | 66 | 57 | -9 | 128 | 92 | -36 |
| `crypto-2` | 91 | 83 | -8 | 105 | 93 | -12 |

回归与噪声（E12 第三轮 -> E13 复跑）：

| 用例 | E12 timer | E13 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `h-1-03` | 298 | 322 | +24 | E12 阶段本身也在该用例有波动；总体收益覆盖 |
| `crypto-1` | 121 | 140 | +19 | run1 为 110 ms，复跑波动较大，继续观察 |
| `01_mm3` | 102 | 112 | +10 | 小幅回退，wall 同向 +9 ms |
| `01_mm2` | 163 | 169 | +6 | 小幅回退，低于整体收益 |
| `h-1-01` | 51 | 56 | +5 | 小幅回退，wall 受运行噪声影响更明显 |

结论与下一步：

1. E13 直接承接 E12 的副作用：E12 把栈流量变成机器 copy，E13 再让 RA 尝试把 copy pair 同色并由 lowering 删除冗余 move。
2. 两次性能均通过且均优于 E12 中位，说明该优化不是单用例偶然收益；第一轮 `7196/11070` 还优于 E12 三次中的最佳 timer。
3. 后续可继续沿机器级承载方向推进：更完整的 copy chain 合并、post-RA dead-copy 清理、或重新评估更窄边界的 load forwarding/loop invariant load，避免把 IR 优化收益输给寄存器压力。

### E14 Copy affinity coloring

状态：已提交 `7b43830`。

参考动机：

- E13 只是在干涉图里放开 COPY 源/目标之间的单条自身冲突边，让二者“可以同色”；但图着色阶段仍按固定物理寄存器顺序选色，很多 copy pair 即使不冲突，也未必会主动选成同一个寄存器。
- Iroha 的 graph coloring RA 有显式 coalescing worklist，Hy/Sisyphus 也会通过后端 combine/RegAlloc peephole 消化 copy chain。我们当前不做图收缩，先补一个风险更小的 affinity 颜色偏好。
- E13 后热点中剩余收益主要来自把机器 `COPY/mv` 继续压低，而不是再大幅改 IR 形态。

实现要点：

1. 干涉图新增 `affinities`，记录同 register class 的简单 `COPY dst(vreg def), src(vreg use)` 双向亲和关系。
2. 继续保留 E13 的安全边界：只跳过 copy pair 自身干涉边，不删除其他 live-out 冲突。
3. 图着色弹栈选色时，若某个 affinity partner 已经着色，且 partner 颜色在当前节点候选集合内、没有被真实干涉邻居占用，则优先选择该颜色。
4. 若没有可复用的 partner 颜色，回退到原有固定候选寄存器顺序；spill、call-live、callee-saved 保存/恢复、frame layout 均不改变。

安全边界：

- affinity 只改变“多个合法颜色里先试哪个”，不会绕过真实干涉边、物理寄存器 forbidden color 或 call-live 只能用 callee-saved GPR 的约束。
- 若 copy pair 在别处仍存在真实干涉边，partner 的颜色会出现在 `used` 集合中，不能被当前节点选择。
- 如果 affinity partner 使用了 `s*`，说明该寄存器已经会被保存/恢复；同色不会新增 ABI 保存成本。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e14-copy-affinity.csv` | 通过 | 140/140 | 60/60 | 7293 | 11393 |
| `perf3-e14-copy-affinity-rerun.csv` | 通过 | 140/140 | 60/60 | 6822 | 10906 |

相邻对比：

| 对照 | timer | wall | E14 两次均值 timer 提升 | E14 两次均值 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E13 两次均值 | 7370 | 11386 | 约 -313 ms（-4.24%） | 约 -236 ms（-2.07%） |
| E13 复跑 | 7544 | 11701 | E14 复跑 -722 ms（-9.57%） | E14 复跑 -795 ms（-6.79%） |

关键用例收益（E13 复跑 -> E14 复跑）：

| 用例 | E13 timer | E14 timer | Δtimer | E13 wall | E14 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `shuffle1` | 318 | 221 | -97 | 909 | 737 | -172 |
| `many_mat_cal-1` | 348 | 271 | -77 | 363 | 286 | -77 |
| `many_mat_cal-2` | 338 | 271 | -67 | 354 | 284 | -70 |
| `many_mat_cal-3` | 329 | 263 | -66 | 342 | 275 | -67 |
| `sl1` | 203 | 148 | -55 | 217 | 158 | -59 |
| `transpose2` | 253 | 207 | -46 | 263 | 219 | -44 |
| `crypto-1` | 140 | 101 | -39 | 150 | 111 | -39 |
| `knapsack_naive-2` | 730 | 694 | -36 | 739 | 702 | -37 |
| `h-1-03` | 322 | 288 | -34 | 331 | 299 | -32 |
| `conv2d-1` | 805 | 776 | -29 | 817 | 788 | -29 |

回归与噪声（E13 复跑 -> E14 复跑）：

| 用例 | E13 timer | E14 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `h-4-03` | 205 | 222 | +17 | 局部回退，但 E14 run1 为 204 ms，判断为波动/选色局部差异 |
| `h-1-01` | 56 | 65 | +9 | 小用例波动；总体收益覆盖 |
| `h-1-02` | 4 | 8 | +4 | 小用例波动 |
| `conv2d-3` | 73 | 74 | +1 | 噪声级 |

结论与下一步：

1. E14 是 E13 的自然补全：先让 copy pair 可以同色，再让颜色选择主动尝试同色。
2. 两轮均通过且两轮均值明显优于 E13 两轮均值；第二轮在 `shuffle1`、`many_mat_cal-*`、`sl1`、`transpose2` 上收益集中，符合“减少机器 move 压力”的预期。
3. 下一步如果继续后端方向，应转向 post-RA dead-copy/branch bool combine 或更完整的机器级 DCE；若收益开始变窄，则回到 A/B 类内存和标量全局优化，避免只在 copy 上打转。

### E15 Cross-block compare-branch fusion

状态：已提交 `7bc099f`。

参考动机：

- 早期 compare-branch fusion 只在 `icmp` 和 `br` 位于同一基本块时跳过分支端 bool 使用；若同一个 `icmp` 同时被多个分支使用，selector 仍会先物化 `slt/xori/seqz` 结果并写回栈槽，再让同块分支重建比较。
- 高分项目的后端 InstCombine 会把 `seqz/snez + beq/bne` 还原成直接分支；我们更适合在 selector 层提前避免生成只供分支使用的 bool 临时值。
- E12-E14 已经降低了 Phi/copy 压力，此时继续清理剩余栈槽 bool reload/store，有机会让分支密集型和递归基线判断用例继续下降。

实现要点：

1. 将 `ICmp` 的 materialization 跳过条件从“只被同块条件分支使用”放宽为“所有 use 都是条件分支”。
2. `translateBranch` 对这种 branch-only `icmp` 即使定义在其他基本块，也直接用 `beq/bne/blt/bge` 重建比较分支。
3. 为了配合 E3 的 local value cache，新增 `crossBlockBranchCompareOperands`：如果跨块 fused branch 需要读取某个比较操作数，而该操作数是在其他块定义的指令，则该操作数不能被判定为 local-only，必须保留栈槽 store/reload 路径。
4. 本阶段只处理整数 `icmp`；浮点 `fcmp` 暂不动，避免 FPR 条件码物化、ABI 和分支重建同时变化。

安全边界：

- 只有 `icmp` 的所有 use 都是条件分支时才跳过 bool 结果生成；如果它还参与 `zext`、store、phi、call 参数等普通数据流，仍走旧 materialization 路径。
- 跨块融合不会绕过 SSA 可达性：分支使用的 `icmp` 操作数若来自其他块，会强制保留栈槽可 reload。
- 对同块分支和跨块分支统一走现有 `loadBranchOperand`，常量 0 仍使用 `zero` 寄存器。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e15-cross-block-branch-fusion.csv` | 通过 | 140/140 | 60/60 | 6887 | 10763 |
| `perf3-e15-cross-block-branch-fusion-rerun.csv` | 通过 | 140/140 | 60/60 | 6674 | 10484 |

相邻对比：

| 对照 | timer | wall | E15 两次均值 timer 提升 | E15 两次均值 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E14 两次均值 | 7058 | 11150 | 约 -277 ms（-3.93%） | 约 -526 ms（-4.72%） |
| E14 复跑 | 6822 | 10906 | E15 复跑 -148 ms（-2.17%） | E15 复跑 -422 ms（-3.87%） |

关键用例收益（E14 复跑 -> E15 复跑）：

| 用例 | E14 timer | E15 timer | Δtimer | E14 wall | E15 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `knapsack_naive-1` | 738 | 697 | -41 | 746 | 706 | -40 |
| `h-4-03` | 222 | 186 | -36 | 231 | 194 | -37 |
| `knapsack_naive-3` | 724 | 696 | -28 | 734 | 705 | -29 |
| `shuffle1` | 221 | 202 | -19 | 737 | 750 | +13 |
| `transpose2` | 207 | 190 | -17 | 219 | 200 | -19 |
| `h-1-01` | 65 | 49 | -16 | 81 | 58 | -23 |
| `01_mm3` | 102 | 88 | -14 | 127 | 104 | -23 |
| `sl2` | 283 | 274 | -9 | 292 | 284 | -8 |
| `transpose1` | 27 | 19 | -8 | 53 | 29 | -24 |

回归与噪声（E14 复跑 -> E15 复跑）：

| 用例 | E14 timer | E15 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `crypto-1` | 101 | 110 | +9 | 小幅回退；首轮 E15 为 99 ms，判断为波动 |
| `crypto-2` | 71 | 77 | +6 | 小幅回退；首轮 E15 为 70 ms |
| `h-1-03` | 288 | 290 | +2 | 噪声级 |
| `h-10-03` | 54 | 56 | +2 | 噪声级 |
| `crc3` | 1 | 2 | +1 | timer 下限附近波动 |

结论与下一步：

1. E15 把 compare-branch fusion 从同块扩展到 branch-only `icmp` 的全部分支 use，减少了 bool 栈槽 store/load 和冗余比较物化。
2. 两轮性能均通过，复跑相对 E14 最好轮仍有明确正收益，满足提交标准。
3. 后续可继续考虑 FPR compare branch 或 post-RA dead-copy，但若后端收益开始变窄，应回到 A/B 类内存优化和标量全局优化。

### E16 Compact SP-relative epilogue

状态：已实现，待本提交入库。

参考动机：

- E15 后重新扫描 2026 perf 汇编，剩余 `mv` 仍有 2595 条，其中大量来自固定返回模板：`ld old_fp, -16(fp); mv sp, fp; mv fp, old_fp`。
- Hy/Sisyphus 的 RISC-V 后端以 `sp` 为栈帧基准生成 prologue/epilogue，返回路径直接释放栈帧；我们当前为方便访问栈槽保留 `fp`，但返回点仍可对小/中栈帧使用 `sp`-relative restore。
- 这个优化不是继续扩展 copy peephole，而是修正后端 frame lowering 的结构模板：让返回路径少一个临时寄存器、少两条 `mv`，同时不改变函数体内仍以 `fp` 访问栈槽的模型。

实现要点：

1. `translateReturn` 对初始 frame size 和 `ra/fp` 保存 offset 均在 signed 12-bit 内的函数，生成 `ld ra, frameSize-8(sp); ld fp, frameSize-16(sp); addi sp, sp, frameSize; ret`。
2. 大栈帧保留旧的 `fp`-relative 模板，避免第一次 legalize 提前把 `sp` offset 拆成 `li/add/ld` 后，callee-saved spill 扩帧时无法重写 offset。
3. `rewriteFrameSetup` 在 callee-saved spill 增加 frame size 后，同步重写 compact epilogue 的 `sp`-relative `ra/fp` offset 和正向 `addi sp, sp, frameSize`。
4. `MachineLegalizer::isFrameSetupInstruction` 只把负向 `addi sp, sp, -frameSize` 视为入口 frame setup，避免入口块里同样形态的正向 epilogue `addi` 被跳过 legalize。
5. `insertCalleeSavedEpilogues` 的 restore point 识别接受 `ld ra, offset(sp)`，确保使用 `s*` 的函数仍在恢复 `ra/fp` 前恢复 callee-saved 寄存器。

安全边界：

- 只在初始 frame/offset 可直接编码时启用 compact epilogue；大 frame 仍使用旧路径。
- 函数体内 stack slot、spill slot 和 caller stack arg 访问方式不变，仍以 `fp` 为稳定基准。
- 若后续 callee-saved spill 扩大 frame，返回点 offset 会在 RA frame rewrite 阶段统一修正。

实测结果：

| 文件 | 构建 | 功能测试 | 性能测试 | timer 总和 | wall 总和 |
| --- | --- | --- | --- | ---: | ---: |
| `perf3-e16-compact-epilogue.csv` | 通过 | 140/140 | 60/60 | 6656 | 10672 |
| `perf3-e16-compact-epilogue-rerun.csv` | 通过 | 140/140 | 60/60 | 6434 | 10148 |

相邻对比：

| 对照 | timer | wall | E16 两次均值 timer 提升 | E16 两次均值 wall 提升 |
| --- | ---: | ---: | ---: | ---: |
| E15 两次均值 | 6781 | 10624 | 约 -236 ms（-3.47%） | 约 -214 ms（-2.01%） |
| E15 复跑 | 6674 | 10484 | E16 复跑 -240 ms（-3.60%） | E16 复跑 -336 ms（-3.20%） |

汇编结构变化：

| 指标 | E15 | E16 | 变化 |
| --- | ---: | ---: | ---: |
| `mv` 总数 | 2595 | 1923 | -672 |
| `ld ra, -8(fp)` | 336 | 0 | -336 |
| `ld ra, offset(sp)` | 0 | 336 | +336 |

关键用例收益（E15 复跑 -> E16 复跑）：

| 用例 | E15 timer | E16 timer | Δtimer | E15 wall | E16 wall | Δwall |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `many_mat_cal-1` | 284 | 254 | -30 | 297 | 265 | -32 |
| `knapsack_naive-1` | 697 | 667 | -30 | 706 | 676 | -30 |
| `shuffle1` | 202 | 175 | -27 | 750 | 686 | -64 |
| `knapsack_naive-3` | 696 | 670 | -26 | 705 | 678 | -27 |
| `many_mat_cal-2` | 285 | 263 | -22 | 298 | 274 | -24 |
| `knapsack_naive-2` | 689 | 668 | -21 | 698 | 676 | -22 |
| `01_mm2` | 149 | 133 | -16 | 168 | 153 | -15 |
| `sl3` | 33 | 18 | -15 | 44 | 28 | -16 |
| `crypto-1` | 110 | 98 | -12 | 121 | 107 | -14 |
| `shuffle2` | 58 | 48 | -10 | 223 | 210 | -13 |

回归与噪声（E15 复跑 -> E16 复跑）：

| 用例 | E15 timer | E16 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `conv2d-2` | 211 | 239 | +28 | E16 首轮为 216 ms，判断为复跑噪声/局部布局差异；总体收益覆盖 |
| `sl2` | 274 | 280 | +6 | 小幅回退，低于整体收益 |
| `conv2d-1` | 771 | 773 | +2 | 噪声级 |
| `h-10-02` | 27 | 29 | +2 | 噪声级 |
| `matmul3` | 111 | 112 | +1 | 噪声级 |

结论与下一步：

1. E16 把小/中栈帧返回路径收短，并解决 E12-E15 后仍大量残留的固定 `mv` 模板问题。
2. 两轮性能均通过，第二轮相对 E15 复跑同时降低 timer 和 wall，满足提交标准。
3. 后端 frame 模板收益仍有边界；下一步不宜继续只追逐 `mv`，应回到 A/B 类内存和标量全局优化，优先找能覆盖矩阵/卷积/knapsack 的跨块 memory forwarding 或更完整 GVN/ADCE。

### E17 自递归全局 reload 前递候选

状态：已验证但暂不采用，代码已回退。

动机：

1. E11 的 call-aware DLE 证明 `knapsack_naive-*` 中 `weight[i - 1]` 的跨 call reload 可被消除，但当时非 exact pointer identity 跨块传播范围过大，矩阵热点 live range 变长，整体回退。
2. 本候选将范围收窄到“自递归函数 + 同一个全局数组 GEP 指针 + callee 不写该全局”的场景，目标只命中 `knapsack_naive`：
   - `%lhs.3 = load weight[i - 1]`
   - 递归调用 `knapsack_naive(i - 1, w)`
   - 再次 `load weight[i - 1]`
3. 普通调用、非自递归函数、未知 store 仍作为屏障，避免影响矩阵、卷积等非递归热点。

实现结果：

| 项目 | 结果 |
| --- | --- |
| 构建 | 通过 |
| 功能测试 | `compiler2026-riscv-function` 140/140 |
| 性能测试 | 60/60 |
| PERF 文件 | `/workspace/Testing/perf3-e17-recursive-global-reload-forward.csv` |
| timer / wall | `6926 / 11069` |
| 对比 E16 首轮 | `+270 ms timer`，`+397 ms wall` |
| 对比 E16 复跑 | `+492 ms timer`，`+921 ms wall` |

关键退化：

| 用例 | E16 复跑 timer | E17 候选 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `knapsack_naive-1` | 667 | 771 | +104 | reload 消除后，值跨递归 call 存活，寄存器压力和保存恢复成本超过减少的 load |
| `knapsack_naive-2` | 668 | 729 | +61 | 同上 |
| `knapsack_naive-3` | 670 | 736 | +66 | 同上 |
| `shuffle1` | 175 | 228 | +53 | 运行噪声/布局扰动也存在，但主退化来自 knapsack |

结论：

1. 只看 IR load 数减少不够，递归 call 两侧的 live range 成本必须纳入决策。
2. 这个切片虽然比 E11 更窄，仍会把热点值延长到递归调用之后，当前后端无法稳定承接。
3. 后续若重启 memory forwarding，应优先选择“不跨 call”的短距离 forwarding，或配合更强的寄存器压力估计；否则转向 B2 的 GVN/ADCE 更稳。

### E18 ABI 参数/返回寄存器直写候选

状态：已验证但暂不采用，代码已回退。

动机：

1. 当前调用 lowering 对寄存器参数常见生成 `li/lw/ld tmp; mv aN,tmp`，返回值也常见 `load tmp; mv a0,tmp`。
2. `a0-a7/fa0-fa7` 不在图染色分配集合内，理论上可以直接把 `loadValueTo` 的目的寄存器设为 ABI 参数/返回寄存器，删除中间 vreg 和 `COPY`。
3. 该候选不跨 call 保留新值，也不改变栈传参，预期是一个局部后端模板缩短。

静态效果：

| 采样项 | E16 | E18 候选 |
| --- | ---: | ---: |
| `conv2d-1` 指令数 | 499 | 489 |
| `knapsack_naive-1` 指令数 | 134 | 122 |
| `shuffle1` 指令数 | 320 | 310 |
| 9 个热点采样 `li/lw/ld tmp; mv aN,tmp` | 73 | 0 |

实现结果：

| 项目 | 结果 |
| --- | --- |
| 构建 | 通过 |
| 功能测试 | `compiler2026-riscv-function` 140/140 |
| 性能测试 | 60/60 |
| PERF 文件 | `/workspace/Testing/perf3-e18-abi-direct-arg-return.csv` |
| timer / wall | `6762 / 10902` |
| 对比 E16 首轮 | `+106 ms timer`，`+230 ms wall` |
| 对比 E16 复跑 | `+328 ms timer`，`+754 ms wall` |

关键退化：

| 用例 | E16 复跑 timer | E18 候选 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `shuffle1` | 175 | 209 | +34 | 静态删 copy 不等于运行收益，物理参数寄存器定值位置改变后整体布局/选色变差 |
| `conv2d-1` | 773 | 806 | +33 | 热点循环外调用模板变短，但主循环未受益，整体受布局扰动影响 |
| `knapsack_naive-1` | 667 | 698 | +31 | 递归调用参数路径变短，但调用密集路径仍变慢 |
| `sl2` | 280 | 310 | +30 | 多维数组热点同步退化 |

结论：

1. 这个候选能稳定减少汇编指令和 `mv`，但广泛用例 timer/wall 同步变差，不满足提交标准。
2. 直接改 ABI 物理寄存器定值会改变后端 liveness/forbidden-color 的局部形态；当前 RA 对这种小模板变化仍敏感。
3. 后续若继续参数/返回模板优化，应先在机器级做 post-RA peephole，并以“已分配物理寄存器后的 no-op/相邻 copy 删除”为边界，而不是提前改变 selector vreg 形态。

### E19 Post-RA ABI move peephole 候选

状态：已验证但暂不采用，代码已回退。

动机：

1. E18 证明 selector 阶段直写 ABI 参数/返回寄存器会扰动 RA，但其静态删指令信号真实存在。
2. 本候选改为 RA 之后在最终汇编上做相邻 peephole：例如 `li t0, imm; mv a0, t0` 改为 `li a0, imm`。
3. 为避免误删仍需使用的物理寄存器，只在源寄存器从该 `mv` 之后到下一次定义、`call`、`ret` 之前不再被读取时折叠；遇到 label/branch/jump 则放弃。

静态效果：

| 采样项 | E16 | E19 候选 |
| --- | ---: | ---: |
| `conv2d-1` 指令数 | 499 | 489 |
| `knapsack_naive-1` 指令数 | 134 | 122 |
| `shuffle1` 指令数 | 320 | 310 |
| 9 个热点采样相邻 ABI move producer | 73 | 0 |

实现结果：

| 项目 | 结果 |
| --- | --- |
| 构建 | 通过 |
| 功能测试 | `compiler2026-riscv-function` 140/140 |
| 性能测试首轮 | 60/60 |
| 性能测试复跑 | 60/60 |
| PERF 文件 | `/workspace/Testing/perf3-e19-postra-abi-move-fold.csv`、`/workspace/Testing/perf3-e19-postra-abi-move-fold-rerun.csv` |
| 首轮 timer / wall | `6687 / 10635` |
| 复跑 timer / wall | `6631 / 10632` |
| 两轮均值 | `6659 / 10633.5` |
| E16 两轮均值 | `6545 / 10410` |

关键观察：

| 用例 | E16 复跑 timer | E19 复跑 timer | Δtimer | 判断 |
| --- | ---: | ---: | ---: | --- |
| `conv2d-1` | 773 | 797 | +24 | 指令数减少未覆盖热点布局/缓存扰动 |
| `many_mat_cal-3` | 266 | 289 | +23 | 矩阵热点仍偏慢 |
| `shuffle1` | 175 | 206 | +31 | 与 E18 一致，shuffle 对该模板变化不受益 |
| `sl1` | 141 | 146 | +5 | 小幅退化 |
| `sl2` | 280 | 277 | -3 | 局部略有收益但不足以抵消总分 |

结论：

1. Post-RA peephole 比 E18 稳定，首轮 wall 略优于 E16 首轮，复跑 timer 也略优于 E16 首轮；但相对 E16 两轮均值仍退化。
2. 该方向收益不足以作为提交项。最终瓶颈不在这类相邻 ABI move，而在循环/内存热点和 RA 对布局的小扰动。
3. 后续不再围绕 ABI 参数 move 做小规则；应回到 B2 的标量清理或 A 类不跨 call 的内存短距离优化。
